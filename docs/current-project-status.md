# Current Project Status

Snapshot date: 2026-07-21

## Current State

- The project has a Web/API implementation for an online stock-selection Agent, AlphaSift screening, industry board data integration, and a vn.py-style paper trading page.
- The Windows Desktop build now has an explicit opt-in vn.py bundle. A Python 3.13.14 build collected vn.py, TA-Lib, and `DsaSimulatedGateway`; the frozen backend passed its import probe and a live `/health` plus `/api/v1/vnpy-paper/status` check with `mode=vnpy_runtime`, a connected `DSA_SIM` gateway, four registered EventEngine callbacks, and a live scheduler loop. Electron then produced a 220.6 MiB NSIS installer and a 655.0 MiB unpacked application whose bundled backend hash matched the validated artifact. The default lightweight Desktop bundle remains unchanged, and no external broker gateway is implied by this evidence.
- Docker now exposes the same default-lightweight, explicit opt-in contract through `INCLUDE_VNPY=true` and Compose `DSA_INCLUDE_VNPY_DOCKER=true`. GitHub Actions acceptance run `29772471589` built both Linux images, started the optional image, and passed live health/runtime gates with `mode=vnpy_runtime`, connected `DSA_SIM`, four registered EventEngine callbacks, zero handler failures, and a live scheduler loop. The default image measured 1,459,313,565 bytes; the standards-compliant vn.py 4.4.0 image measured 2,229,015,999 bytes, a 769,702,434-byte increase that retains vn.py's declared PySide6 and analysis dependencies. The manual workflow publishes no image and uses no credentials or external gateway.
- The paper trading path works without a real vn.py installation by using the local Portfolio ledger; `vnpy_available=false` does not block local paper trading.
- Automatic paper trading can run from AlphaSift candidates through risk checks into local paper orders, with dry-run, manual approval, `paper`, and optional `vnpy_paper` execution modes.
- Persistent full-market factor jobs now reconcile their database lease with the current process task queue. API responses classify jobs as `active`, `orphaned`, `retryable`, or `complete`; an ordinary resume safely takes over a task lost across process restart, while a genuinely active task remains protected by HTTP 409. The Agent console exposes the same state and only offers recovery for orphaned or failed jobs. `scripts/check_full_market_ingestion_e2e.py` provides a default-read-only acceptance gate for scale, checkpoint completion, factor coverage, source errors, regressions, and stalls; creating an external job requires an explicit permission flag.
- The dated-universe path no longer has a Tushare `stock_basic` permission single point. It prefers Tushare, then composes BaoStock Shanghai/Shenzhen lifecycle rows with the BSE official current list, code-mapping table, and final delisting notices. The BSE source fails closed for any current/mapping/delisting mismatch, and the combined result is cached for 24 hours. A live 2026-07-17 request returned 5,529 untruncated members across SSE/SZSE/BSE; the one historical BSE delisting is present before its final trading date and absent afterward.
- A clean 2026-07-17 full-market job completed all 5,529 work items in 111 checkpoints. The acceptance gate passed with 5,494 complete factor rows (99.367%), 5,522 exact-date daily rows (99.8734%), 5,502 valuation-complete rows (99.5117%), and 27 audited source errors (0.4883%). The job persisted `baostock+bse.official_lifecycle` as its actual universe source and finished with `recovery_state=complete`.
- Implemented corporate actions now prefer Tushare and fall back to AkShare EastMoney, CNInfo, then official BSE implementation-announcement PDFs, with cached symbols and actual-provider source keys. A live default-route ingestion for `600519` persisted three cash dividends with zero errors: 27.673 per share on 2025-06-26, 23.957 on 2025-12-19, and 28.02423 on 2026-06-26. An independent normalization of both SSE/SZSE free providers found 34 common date/type events and zero value differences. For BSE, the parser accepted all latest 20 implementation announcements sampled from the official endpoint and uses the official current-to-legacy code mapping to retrieve pre-920-code history.
- After the final API restart, background task `ff2c2bb67d194596811efd53ace3ad42` completed a `920976` ingestion through the public endpoint with one complete factor row, four idempotent corporate-action updates, and zero errors. The stored events include the 2024-06-06 CNY 0.60 per-share cash dividend and 1.4 holding multiplier, plus two later cash dividends. Its methodology reported the full provider route, `bse.companyAnnouncement.pdf` as the source actually used, and SSE/SZSE/BSE as verified free coverage. DSA_SIM remained connected, automatic paper trading remained enabled, and all four EventEngine callbacks remained registered.
- A read-only production-calibration acceptance gate now turns the existing 7/30/90-day trend API into explicit evidence criteria. `scripts/check_agent_calibration_evidence.py` defaults to `trigger_source=agent_calibration_shadow`, so manual and ordinary auto-trade runs cannot inflate calibration evidence. It checks each required market for completed-run volume, observed objective snapshots, observation rate, latest mature forward sample count, distinct observation days, freshness, objective versions, truncation, and non-deployable states without creating a run or order. After a 2026-07-21 three-market task completed in 114.977 seconds with three candidates per market, two dry-run plans, and zero submissions, the shadow-only gate correctly fails with CN at 7/7 observed runs across two observation days, HK at 6/6 across one day, and US at 8/8 across one day; all three have 100% observation rate and zero mature samples. The gate left the total shadow-run count unchanged at 23. This is the authoritative current production-calibration gap rather than a claim of completion.
- A default-off `agent_calibration_shadow` scheduler can now accumulate production-shaped evidence without submitting orders. It accepts explicit `market:strategy` pairs, validates each pair against AlphaSift `market_scope` before creating a run, forces dry-run and zero-order behavior, disables dynamic strategy replacement and account/order side effects, shares a non-blocking lock with formal automation, and keeps its run history separate from formal last-run, alert, LLM-circuit, and failure-fuse state. DSA overlays `us_large_cap_momentum` and adds an explicit `hk_liquid_momentum` compatibility screen, so the deployed configuration can sample CN, US, and HK without modifying AlphaSift's installed package.
- Calibration shadow scheduling now enforces its configured interval from the persisted latest completed run for each source/market/strategy pair, so a process restart cannot add another sample after the five-minute startup delay. Expected no-ops persist bounded cadence-skip evidence with age, remaining time, and next eligibility; a maximum five-minute grace absorbs sequential multi-market runtime and scheduler jitter without turning restart samples into independent evidence. Explicit manual dry-runs remain available.
- The restarted live API proved that guard twice. The final startup task at 2026-07-21 04:00:40 completed its cadence decision in 0.02 seconds with zero attempts, candidates, plans, submissions, or failures and three audited skips. CN, US, and HK referenced their exact latest run UIDs and next eligibility times between 2026-07-22 02:49 and 02:51. A read-only gate immediately afterward retained exactly CN 9, US 10, and HK 8 completed shadow runs, proving that neither restart inflated the sample counts; the gate still correctly fails on observation-day and mature-forward-sample requirements.
- The same gate is now a scheduler-owned read-only monitor named `agent_calibration_evidence`. It starts 600 seconds after registration, follows the shadow interval, and records bounded pending/ready evidence without creating Agent runs, plans, or orders. A live repository-backed invocation registered all four paper tasks and retained exactly CN 9, HK 8, and US 10 runs before and after evaluation. It correctly reported pending with 11 explicit threshold failures; this automates gap visibility without manufacturing calibration samples.
- The deployed scheduler executed its first `agent_calibration_evidence` task at 2026-07-21 04:30:55 in 0.028 seconds. Its persisted terminal event reported `read_only=true`, `creates_agent_runs=false`, `places_orders=false`, all three markets, and the same 11 pending failures. A new read-only `/agent-runs/calibration-evidence` API and responsive Agent-console panel expose this exact contract online; live API evidence remains CN 9/9 observed, HK 8/8, and US 10/10, all with 100% observation rate and zero mature samples. Desktop 1440x900 and mobile 390x844 browser checks found no page overflow; mobile uses per-market details instead of compressing the six-column table.
- Calibration evidence readiness now has unattended transition alerting. The monitor persists its initial state and only later ready/pending changes under the existing `vnpy_paper` alert target, routes notifications through the existing alert channels, and uses persisted history for deduplication across restarts. Pending is recorded as `degraded`, ready as `resolved`, and alert failures remain isolated from the read-only task. This completes operational visibility, but it does not satisfy the still-missing mature forward samples or external broker evidence.
- Live transition acceptance started with 47 Agent runs, 126 trade plans, 22 portfolio trades, and no prior calibration-evidence alert. The first pending evaluation added exactly one `degraded` alert; an immediate second evaluation detected `state_unchanged` and added none. All three trading counts remained unchanged. The configured Feishu route returned `Bot Not Enabled`, while the evidence monitor still completed successfully with 11 expected threshold failures, proving notification isolation. The API was then restarted into `vnpy_runtime` with connected `DSA_SIM`, four registered EventEngine callbacks, and the persisted pending baseline available for cross-restart deduplication.
- The calibration-evidence API and Agent console now expose the latest transition alert and sanitized delivery attempts. The live endpoint reports the persisted pending alert as `failed`, with one Feishu attempt, zero successes, one retryable failure, and `send_failed`; the page renders the same channel result instead of silently implying delivery. Alert-history failure is isolated as `unavailable`. The post-change API remains in `vnpy_runtime` with connected `DSA_SIM` and four callbacks. Desktop browser QA at the available 1280x720 viewport found no horizontal overflow or console warnings/errors; the browser viewport override did not change size, so this change does not claim a new mobile-browser run beyond responsive unit coverage.
- Calibration-alert delivery now has bounded unattended recovery inside the existing `vnpy_paper_auto_retry` task. Retry progress is reconstructed from persisted per-channel attempt numbers, retained across restarts, retried at roughly five-minute intervals, and stopped after three dispatch rounds; a five-second due tolerance prevents a fixed scheduler cadence from running just before the persisted deadline forever. The first live restart advanced the old Feishu failure from `#1` to `#2` and exposed `waiting 2/3`; the cadence boundary then reproduced the one-second early-run defect, and the corrected restart advanced to `#3`, returned `exhausted 3/3`, and cleared `next_retry_at`. All three attempts remain `send_failed` because the external bot is still disabled. The task isolates alert-history exceptions from order recovery and never runs the Agent or creates plans/orders. Live counts remained 47 Agent runs, 126 trade plans, and 22 portfolio trades; only notification audit rows increased. The 1280x720 Agent console rendered all three attempts and the exhausted state without horizontal overflow.
- Completed manual LLM recaps now close a bounded feedback loop only when the operator has explicitly enabled dynamic planning. Dynamic-plan prompt v3 reads at most three recaps from the same trigger source, strategy, and market as untrusted audit observations; recap text cannot override the system contract, while the existing evaluator prevents candidate-count or per-order-cash increases and minimum-score decreases. Recent-run context schema v3 also fixes the previous implementation mismatch that always queried formal auto runs even when the caller requested calibration-shadow context.
- The first real US calibration shadow run `ss-agent-20260720165408-d91ad2b9` completed with three online candidates, one audited dry-run plan, zero submitted orders, and two position-limit skips. When yfinance was rate-limited, the request-scoped snapshot fallback obtained all ten configured US large-cap quotes from Tencent. The first cross-currency plan also refreshed CNY/USD on demand and converted CNY 10,000 to USD 1,477.80; missing or stale rates still fail closed.
- The first real HK calibration shadow run `ss-agent-20260720171834-1109843a` completed in 25.85 seconds with three candidates, one HKD dry-run plan, zero submitted orders, and two position-limit skips. Its bounded 15-symbol snapshot used Tencent direct quotes for every row; a single HK quote completed in 0.055 seconds compared with the previous full-market AkShare request timing out after 60 seconds. The compatibility screen reuses AlphaSift's strategy parser, hard filters, factor scorer, and market-independent LLM ranker without modifying the installed package.
- A live HK LLM probe now preserves deterministic candidates and structured failure evidence when ranking is unavailable. The 10-second probe returned three Tencent-backed candidates with `quality_status=ok`, `llm_ranked=false`, and an audited timeout parse error. Follow-up calibration shadow `ss-agent-20260720174258-9947ae66` reused the Agent's persisted circuit-open decision, produced one dry-run plan, and submitted zero orders, confirming that HK ranking failures degrade once and do not bypass the existing cross-run circuit breaker.
- The post-change API is running on `127.0.0.1:8000` under Python 3.13. AlphaSift reports ten strategies and supported markets `cn,hk,us`; `hk_liquid_momentum` is present. The saved automatic execution mode remains `vnpy_paper`, the vn.py runtime is available, DSA_SIM is connected, its reconnect monitor is healthy, and all four EventEngine callbacks are registered.
- The deployed-runtime soak contract now separates callback registration from actual gateway traffic. The EventEngine bridge exposes payload-free per-type observed/handled/failure counts and last-observed timestamps, refreshed on each runtime status request. Soak schema v2 can require exact gateway class/name, reject the built-in DSA_SIM as external evidence, require new account/position/order/trade events within the observation window, fail on counter regression, and cap handler failures. This closes the evidence-definition gap but does not claim that an external broker gateway has been installed or observed.
- A live read-only schema-v2 soak against PID `18420` passed 10/10 API, runtime, connection, and callback-registration samples with zero process/gateway changes, counter regressions, handler failures, or response errors. Re-running the same deployed process with production evidence flags exited 1 with `gateway_identity_mismatch`, `external_gateway_not_confirmed`, and `required_event_observations_below_threshold`, correctly refusing to count DSA_SIM's zero account-event delta as real broker evidence. Neither run created an Agent run, plan, or order.
- On 2026-07-21 the restarted scheduler registered and executed its first valid `cn:dual_low` shadow task in 47.842 seconds. It completed one pair with three candidate decisions and three audited dry-run plan rows; all three were explicitly skipped as existing positions, so planned/submitted counts and task failures were zero. The formal latest-auto-run UID remained unchanged, formal automation remained enabled, and DSA_SIM remained connected with all four EventEngine callbacks.
- Agent run details now support persisted human acceptance feedback (`approved`, `needs_changes`, or `rejected`) with reviewer and notes. Feedback appears in the run list, detail timeline, and daily summary, and is carried into later same-strategy/market run context. A deterministic cross-market objective can use needs-changes/rejected feedback, consecutive failures, or degraded cross-run quality to tighten candidate count and per-order cash; it never widens saved limits or bypasses risk gates.
- Runtime scheduler background tasks are registered independently from the daily analysis job, so `serve-only` / `webui-only` can still run paper auto-trading tasks.
- Runtime scheduler reconcile now reuses process-local locks by task name. A replacement generation records a persisted `task_already_running` skip while the older generation finishes, status exposes `overlap_guarded` and `previous_generation_running`, and exception paths release the lock for the next run.
- Background-task startup execution is now tracked by task name across scheduler generations. `vnpy_paper_auto_retry` runs immediately on initial registration, does not rerun merely because an unrelated configuration update rebuilds the scheduler, and runs once again after the task is disabled and re-enabled. Registration state remains independent between automatic trading, recovery, and event-monitor tasks.
- A read-only scheduler soak CLI can now collect deployment evidence through the existing API. It gates API reachability, scheduler-loop availability, continuous registration of explicitly required tasks, terminal events created after the observation baseline, task failures, and cross-generation `task_already_running` skips without triggering screening, plans, or orders.
- On 2026-07-20, the Python 3.13 API was started with the real vn.py MainEngine/EventEngine and built-in DSA_SIM gateway. A 75-second scheduler soak recorded 74/74 successful API samples, 74/74 live-loop samples, 100% recovery-task registration, one new recovery terminal event, zero failures, and zero overlap skips. The original 1,440-minute automatic interval was restored afterward; DSA_SIM remained connected with all four order/trade/account/position callbacks registered.
- A separate read-only deployed-runtime soak now observes the actual Web/API process instead of creating a competing MainEngine. It gates API/runtime/confirmed-connection/callback availability ratios, contract compatibility, process and gateway identity changes, plus reconnect counter deltas and monotonicity while excluding connection settings and never invoking a write endpoint.
- On 2026-07-20, that gate collected 75/75 successful samples from the Python 3.13 DSA_SIM API. API, runtime readiness, confirmed connection, event-bridge registration, and every order/trade/account/position callback remained at 100%; process changes, response errors, and new reconnect failures were all zero. This proves the deployed simulator process, not a real broker gateway.
- Runtime scheduler tests now replace only the requested `sys.modules` key instead of rolling back the interpreter's complete module table. The complete 27-test file passes in both Python 3.13.14 and Python 3.14.6, eliminating the previous test-induced LiteLLM/Pydantic/tokenizers re-import failure.
- AlphaSift post-rank enrichment now refreshes candidate quality and derives conservative negative trading-status evidence from the final realtime quote. A non-ST quote name, positive volume plus amount, and an A-share absolute move below 4.5% independently support ST, suspension, and price-limit checks; near-limit or incomplete evidence remains unavailable and fails closed. Each derived field records provider, observation time, and derivation basis.
- A Python 3.13 production-shaped dry-run on 2026-07-20 proved the fix against Tencent quotes: before the change all three candidates were rejected as `candidate_trading_status_unavailable`; after a confirmed process restart, all three had complete audited status evidence, one generated a dry-run plan, and two were skipped only because positions already existed. The run submitted zero orders and added zero Portfolio trades.
- The same live Python 3.13 API then completed a controlled online-Agent-to-DSA_SIM fill. The trading-time gate was temporarily disabled inside a restore-guaranteed operation and was confirmed enabled afterward. Three selected candidates produced one vn.py submission and two `position_exists` skips; order `DSA_SIM.1` reached `filled` through the real EventEngine bridge. Portfolio trade count moved from 12 to 13 with a `600015` buy of 1,400 shares at 6.98, cash moved exactly from 27,474 to 17,702, and the new position matched the fill. The gateway remained connected with four callbacks registered.
- A repeatable `scripts/check_online_agent_vnpy_e2e.py` gate now turns that manual acceptance into a machine-verifiable workflow. It defaults to dry-run, restricts order mode to the built-in simulated gateway behind an explicit permission flag, polls Agent plans to terminal state, validates decision/plan/trade links and Portfolio cash/position deltas, and restores any temporary time-gate change in `finally`.
- On 2026-07-20 the new gate passed against the live Python 3.13 API twice. Run `ss-agent-20260720095356-72a024b5` completed a three-candidate dry-run with three traceable decisions/plans and no account mutation. Run `ss-agent-20260720095531-de6c0400` exercised the DSA_SIM order-authorized path with `--allow-no-fill`; all three existing positions were explicitly skipped, cash and positions were unchanged, the time gate was restored, and the connected runtime retained all four callbacks. The earlier `600015` fill remains the positive fill evidence; the all-skipped run is safety/restoration evidence only.
- The gate now supports an isolated positive-fill account lifecycle. It pauses automatic buying, snapshots account IDs, resets to a clean ledger, validates fills against that ledger, restores the original account and settings, and hides the new archived test account. Unknown reset/restore/cleanup responses fail closed with automatic buying left paused. Live run `ss-agent-20260720101013-dd345609` filled all three candidates in temporary account 3; cash changed by exactly -29,538 and position deltas were `600015 +1400`, `600016 +2800`, and `000001 +900`. The script then restored account 2 with its original 17,702 cash and nine positions, restored both automation switches, hid account 3, and retained a connected four-callback runtime.
- A repeated real-EventEngine test exposed a race where a plan reached `filled` while its decision remained `submitted`. Plan and decision execution updates are now atomic, submission and late order callbacks use optimistic `updated_at` checks, and duplicate trade replay repairs a historical lagging decision. The original test passed ten consecutive isolated runs after the fix; the acceptance poll also waits for linked plan/decision coherence instead of plan terminal state alone.
- After restarting the live API onto the fixed code (PID 6904), isolated run `ss-agent-20260720103034-40909a79` produced three filled plans and three matching filled decisions with local trade IDs 17, 18, and 19. Independent reads confirmed account 2 was restored with cash 17,702 and nine positions, both automation switches enabled, temporary account 4 hidden, DSA_SIM connected, and four callbacks registered.
- The same acceptance gate now has a `scheduler` trigger mode that never calls `/auto/run`. It snapshots existing Agent runs, enables a one-minute automatic interval, captures the first new scheduler-created run, immediately disables later intervals, and requires the persisted `vnpy_paper_auto_trade` terminal event to reference that exact run before accepting account deltas. Live isolated run `ss-agent-20260720104205-9066bffd` completed in 37.934 seconds with three candidates, three submissions, and three fills. Cash changed by -29,538 and positions changed by `600015 +1400`, `600016 +2800`, and `000001 +900`; account 2, the 1,440-minute interval, execution mode, time gate, and automatic-trading switch were all restored, temporary account 5 was hidden, and DSA_SIM retained its connected four-callback runtime.
- The API-owned vn.py MainEngine/EventEngine is now injected into Runtime scheduler paper-trading services; the recovery task runs once at registration and then every one to five minutes, so background execution no longer has a separate bridge capability from Web requests.
- vn.py runtime connection diagnostics now distinguish a returned `MainEngine.connect()` request from a gateway-confirmed connection. Connect exceptions degrade to `connect_failed` without aborting API startup, supported gateway hooks refresh on status reads, and an explicitly disconnected gateway blocks new submissions as `vnpy_gateway_disconnected`.
- vn.py runtime now has an opt-in unattended reconnect monitor. It retries only explicit failed/disconnected states at a configurable 5-to-3600-second base interval, rereads the external settings file for each attempt, exponentially backs off consecutive failures to a configurable maximum, and resets after confirmed recovery. A configurable 5-to-600-second confirmation grace period protects accepted asynchronous connections. Diagnostics expose thread state, grace, base/current/maximum intervals, consecutive failures, attempt/success/failure counts, timestamps, last result, and next check without exposing credentials.
- Operators can request one safe reconnect through `POST /api/v1/vnpy-paper/gateway/reconnect` or the Web paper-trading toolbar even when background auto reconnect is disabled. Connected, confirmation-pending, and grace-protected gateways are not logged in again; the action returns connection/reconnect audit only and does not create an Agent run, plan, or order.
- Reconnect transitions now use a separate event queue: the first consecutive failure, first maximum-backoff transition, and recovery persist into the existing `vnpy_paper` alert history and notification route. Repeated failures do not duplicate the open event, recovery is recorded as resolved, shutdown drains queued events, and diagnostics exclude the connection path and credentials.
- The built-in gateway now has default-off deterministic rejection and duplicate-trade-event injection for acceptance testing. The installer fault matrix proves one native rejection produces no trade, two callbacks with the same `vt_tradeid` remain one MainEngine/Portfolio trade, and reconnect faults can be composed in the same real vn.py runtime. Native Chinese vn.py status values are normalized, and an immediate post-persistence terminal reconciliation closes the race where a synchronous gateway event arrives before the Agent plan stores its `vt_orderid`.
- A real MainEngine/EventEngine Agent matrix now submits three candidates through one run and proves `filled / failed / filled` isolation under every-second-order rejection and duplicate trade callbacks. Final run counts are rebuilt from persisted plans after completion, and a concurrent duplicate callback that loses the Portfolio uniqueness race is treated as a successful idempotent no-op.
- Candidate-level missing prices now fail closed only after the independent realtime quote fallback also fails. The skipped result preserves requested-price, resolution-source, candidate-context, and budget audit. A real MainEngine/EventEngine three-candidate matrix proves the unavailable middle quote never reaches the gateway while the first and third candidates still fill exactly once.
- Candidate-context health now persists aggregate quote/fund-flow/news status plus provider-level `quote/<provider>`, `fund_flow/<provider>`, and `news/<provider>` trend evidence. Provider rows are excluded from the current-run quality score to prevent double weighting. Automatic Agent screening combines the latest 30-day same-strategy/market degradation rates with current source state, passes request-scoped fund-flow and news provider orders only to candidate enrichment, and isolates their caches without mutating global source priority. News evidence records every attempted provider, including exceptions followed by successful fallback. News providers now share the persisted 3-failure/5-minute/one-probe circuit-breaker contract used by realtime and fund-flow sources; empty usable results are inconclusive rather than transport failures.
- A generic no-order gateway soak CLI now reads the configured external `VNPY_*` runtime, samples connection state, counts order/trade/account/position events, records transitions and reconnect totals, and exits nonzero for insufficient connected ratio or missing required events. It suppresses the DSA business event bridge and never emits the settings path or payload. An explicit DSA_SIM-only disconnect mode validates the harness without making it available against real gateways.
- A complete isolated Python 3.13.14 environment now contains AlphaSift 0.2.0, LiteLLM 1.91.3, vn.py 4.4.0, and the project dependencies. Its smoke check constructs a real vn.py `OrderRequest`, starts and closes the real EventEngine/MainEngine, and uses the repository-local ignored `.vntrader/` runtime directory.
- Active vn.py plans become eligible for `MainEngine.get_order` / `get_all_trades` reconciliation after a 60-second grace period. Pausing new automatic buys keeps order recovery scheduled, and missed, cancel-race, or post-timeout late fills are still accumulated idempotently into the Portfolio ledger.
- The latest scheduler alignment fix delays the first `vnpy_paper_auto_trade` run to the next trading window when the time gate is enforced and the service starts outside market hours.
- Paper-trading budgets, cash checks, position exposure, and daily usage now use the account base currency. Foreign-market orders convert through the Portfolio FX table, fail closed for new buys when rates are missing or more than seven calendar days old, preserve sell-side risk reduction, and record both base and quote amounts for audit.
- Automatic holding age now pages through complete trade and split histories under the service's 100-row limit and replays open FIFO lots with corporate actions ordered before same-day trades, so a full exit followed by re-entry resets max-holding and no-progress clocks. Unavailable split history leaves age unknown instead of triggering a time-based exit from incomplete evidence. Risk-reducing sells remain recorded in orders, plans, decisions, and run counts when later data-quality, account, or market gates block new buys.
- Agent-run detail now derives a live `portfolio_change` audit from persisted plans backed by Portfolio trade IDs. Both Web detail views, timeline, and JSON export group booked buy/sell/net quantities and notionals by symbol while keeping submitted, unbooked order-level partial fills, dry runs, and approval-only plans visibly pending or planned. Partial, late, approved, and recovered fills appear automatically after ledger booking without persisting a duplicate account snapshot.
- Candidate decisions now persist `strategy_evidence`, `position_plan`, `risk_review`, `agent_review`, and `llm_review` in dedicated columns and expose them as top-level API fields. Order lifecycle callbacks preserve these explanations even when replacing `order_result`; startup migrates existing databases, and legacy rows/Web contracts keep a compatible embedded fallback.
- Automated buys now have default-off intraday and cross-market risk gates. The local gate audits equal-weight directional index changes and, for A shares, live advance/decline breadth; the linked gate uses a versioned market map and excludes VIX. Missing enabled evidence fails closed, sell-side risk reduction still runs first, and the Web exposes all thresholds. Provider quotes do not yet share a uniform as-of timestamp, so production time-alignment calibration remains open.
- Intraday/cross-market evidence calls now have a 15-second per-call budget and a process-wide cap of four outstanding worker threads. Timeouts, provider failures, and exhausted slots produce distinct audited evidence and fail closed without allowing an enabled buy gate to hang indefinitely or spawn unbounded workers.
- Market-index and breadth fallback results now retain the successful provider, local fetch time, and available data date/granularity. TickFlow quote timestamps are freshness-checked, Tushare index_daily rows can no longer masquerade as intraday evidence, stale session dates are rejected for the local intraday gate, and closed-market session bars remain available only to the cross-market context. Efinance/AkShare still lack a uniform provider-side as-of field, which remains an explicit unverified limitation rather than being replaced by the local fetch time.
- Explicit stock and industry target weights now cap each automatic buy to the remaining target gap instead of rejecting the whole candidate. Existing positions can be replenished toward an explicit stock target even when the maximum position count is reached or existing positions are normally skipped; the final amount is reduced to an executable market lot and audited in `target_weight_sizing`.
- Automatic buys now have an optional constrained portfolio-allocation stage with score-weighted, score/inverse-20-day-volatility, correlation-capped, and covariance target-tracking methods. Point-in-time local trailing returns drive the risk methods; missing history or overlap fails closed, and audits include observations, pairwise coefficients or covariance matrices, target vectors, convergence state, caps, intended budget, and executable A-share-lot residual.
- Every AlphaSift auto-trade run now receives a deterministic 0-100 data-quality score weighted by screen integrity (45%), candidate coverage (35%), and observed source health (20%). Source health now includes snapshot/daily providers and independently observed candidate-context quote, fund-flow, and news retrieval. Missing telemetry is marked unobserved at 70 rather than assumed healthy. `auto_min_data_quality_score` defaults to 60, including for missing or `null` legacy values; lower-scoring rounds are blocked as `data_quality_score_below_threshold`, while an explicit 0 keeps soft scoring audit-only without bypassing hard stale/unavailable gates. Candidate rule reviews also carry their own coverage score.
- Candidate missing fields now fail closed when they are required by an enabled buy-side control: an explicitly missing trading status blocks ST/suspension/price-limit evaluation as `candidate_trading_status_unavailable`, and unavailable turnover blocks a configured minimum-turnover check as `liquidity_data_unavailable`. Other missing fields remain scored and audited rather than being rejected indiscriminately.
- The Agent console now runs an on-demand 1/5/10/20-trading-day forward evaluation for persisted buy candidates. It anchors on the recorded decision price, reads only local daily bars strictly later than the decision date, reports coverage and return/excursion metrics, and never reruns a strategy or places an order by default.
- The same forward evaluation now joins persisted rule and LLM review snapshots, including real `action=skip` risk rejections, and builds a long-horizon quality matrix by reviewer/model plus prompt/evaluator version. It reports mature coverage, passed precision, blocked-loss avoidance, passed/blocked average returns, and return spread without treating missing forward bars as success.
- Cross-run quality now derives one effective verdict per candidate with LLM-over-rule precedence. Mature low passed precision or blocked-loss avoidance can make the combined state `blocked`; negative passed return or return spread can make it `guarded`. The existing opt-in gate consumes that combined state, while the always-on market objective can only tighten from it; insufficient evidence remains audit-only.
- Cross-run quality now also computes the versioned `candidate-return-risk-v1` objective from strictly forward candidate daily returns. It exposes daily mean return, volatility, downside deviation, 20% expected shortfall, horizon-scaled downside risk, maximum adverse excursion, and a deterministic utility score. Negative mature utility tightens candidate count and order cash; negative utility plus negative average return becomes `blocked`. The opt-in gate still affects buys only, while sell-side risk reduction runs first.
- Persisted return/risk objective snapshots now have a read-only 1-90-day calibration-trends API and a 7/30/90-day Web view grouped by market, strategy, and objective version. It reports observation coverage, states, mature samples, and utility without claiming overlapping rolling windows as independent samples; legacy runs remain `unknown`.
- AlphaSift now has a persisted point-in-time factor snapshot table and single-date replay APIs. Hard-filter and scoring-field coverage are audited separately, incomplete datasets fail closed, and the Agent console can inspect coverage and run deterministic hard-filter plus `screen_score` replay without current-data fallback or LLM ranking.
- For an explicitly supplied historical universe, a bounded background task now collects daily/turnover and historical valuation factors with strict as-of dates. Daily history uses an audited Sina -> EastMoney -> Tencent -> BaoStock fallback route and records the provider used by every persisted row. The Agent console can then run a cross-date equal-weight or explicit per-symbol target-weight portfolio backtest with per-side costs, benchmark/excess return, drawdown, coverage, and period equity; residual target weight stays in cash and it never places orders.
- Cross-date backtests now retain symbols selected in adjacent snapshots, charge costs only on actual entries/exits, expose period and aggregate turnover, and apply A-share suspension/price-limit gates only when a trade is required.
- A sell blocked by suspension or a limit-down session is now marked to market without fake sell costs and carried into the next rebalance attempt. Period output separates selected coverage from actual/forced holdings, and summary metrics disclose any ending open positions.
- A Tushare-first, free-source-fallback historical-universe endpoint reconstructs A-share membership for a requested date from listing/delisting lifecycle rows, including symbols that later delisted. It fails closed unless SSE, SZSE, and BSE lifecycle coverage is complete, never substitutes the current universe, and discloses that names/industries are not point-in-time fields.
- Persistent full-market ingestion jobs now resolve each snapshot date independently from one lifecycle load, freeze date/symbol work items, checkpoint every completed factor batch, and retain cursor, heartbeat, write counts, and source errors across restarts. Lease-protected force takeover prevents an old executor from advancing a replaced job, and the Agent console restores the latest job after refresh.
- Portfolio replay now defaults through API/Web to a cash ledger: equal target values or explicit per-symbol equity percentages are converted to whole A-share lots, overweight positions sell before underweight buys, buys are capped by cash, and final blocked exits remain marked open positions. Explicit weights total at most 100%, preserve the residual as cash, apply only to configured symbols also selected in that period, and audit configured-but-unselected symbols. Period output audits cash, market value, quantities, target weights, trades, blocks, and traded-notional turnover; the prior mean-return approximation remains opt-in and rejects explicit weights.
- Cash-ledger replay now accepts an absolute per-trade minimum commission and a sell-only tax rate. The same absolute fee function constrains target and cash-affordable lot quantities, while every trade and period plus aggregate metrics disclose execution price, commission, tax, slippage cost, and net cash effect. Defaults remain zero because broker schedules and historical tax regimes are not inferred.
- Cash-ledger replay now accepts explicit cash-dividend and split-adjustment events against unadjusted prices. Per-symbol event clocks advance to trade/mark dates, same-day actions run before trades, dividends apply only to existing shares, and splits update quantities before rebalancing or final exit. Period audits distinguish applied from not-held events and expose share/cash effects; fractional shares settle only when the event provides an explicit cash-in-lieu price and otherwise fail closed.
- The Web paper trading page now surfaces the unified backend `diagnostics.system_health` view, readiness diagnostics, AlphaSift availability, scheduler loop status, task health, task event logs, 7/30/90-day terminal-run stability metrics, trading window status, scheduling-window alignment, valuation degradation hints, position-industry coverage, and the persisted account drawdown latch. The drawdown guard supports a configurable recovery hysteresis, blocks new buys until the recovery threshold is reached, and records a `resolved` system event when it reopens. Full status reports resolved/missing positions, coverage percentage, missing symbols, and industry market values; configured industry limits fail closed on incomplete coverage. The Agent console adds 7/30/90-day cross-run overall quality and per-source degradation trends.
- Full paper-trading system health now aggregates position-price coverage and freshness without additional market-data calls. The valuation component reports available/fresh/missing/stale/unknown counts, source and provider distributions, affected symbols, and the observed price-date range; the Web availability panel renders the same source-health summary.
- Full paper-trading snapshots now persist valuation-source health at most once per 15-minute bucket with 120-day retention. System health and the Web paper-trading page expose real 7/30/90-day observation counts, average/minimum coverage and freshness, degradation rates, and provider usage; lightweight status reads history without creating an observation.
- Valuation trends distinguish total polls from position-bearing health observations. Empty-position snapshots no longer contribute synthetic 100% health; time averages/minima and degradation rates use only effective observations, while position-weighted coverage/freshness and provider shares disclose the exposure-weighted view and provider-attribution coverage.
- Readiness and unified system health now surface the persisted latest automatic-run outcome independently of scheduler task-event retention. The Web availability panel shows its run timestamp/id, original skip or failure reason, and candidate/submission counts; a historical non-success is a warning rather than a current required blocker.
- Paper status now exposes API, contract, build, Python, and process-start metadata. Contract version 3 is the current Web compatibility baseline; the availability panel warns when an older backend omits or reports an earlier contract, making stale API processes visible without relying on a 404 or incidental schema failure.
- Configured account drawdown now uses an observed equity peak persisted per paper account. A profitable account that falls from its peak can block new buys even while remaining above initial cash; full status, system health, alerts, and Agent run diagnostics expose the peak, current equity, drawdown, threshold, and calculation basis.
- Every Agent plan now persists a deterministic summary of the five latest runs for the same trigger, strategy, and market. The optional LLM dynamic planner v2 consumes that exact context, while the Agent console displays recent-run count, historical submission rate, latest status, and failure streak even when LLM planning is disabled.
- Every auto-selection run now derives a bounded cross-run forward-quality state from persisted candidates and strictly later local daily bars. The state audits mature samples, coverage, win rate, return metrics, previous state, and transitions. Its default-off gate blocks only new buys when mature performance is below threshold or evaluation is unavailable; insufficient evidence stays non-blocking and automated sell risk checks remain active.
- The read-only `/agent-runs/cross-run-quality` endpoint and Agent console expose that current configured state before the next scheduled run, without creating audit rows, refreshing data, or placing orders.
- Automated Agent screens now persist complete AlphaSift source-health snapshots and compute a bounded 30-day health-weighted snapshot-source order for the same strategy and market. Explicit source priorities remain authoritative; each run and the Agent console expose the base order, effective order, adjustment decision, and per-source weights.
- Automated AlphaSift LLM ranking now defaults to a separate 45-second request budget and zero structured-output retries, configurable through `VNPY_AUTO_ALPHASIFT_LLM_TIMEOUT_SEC` and `VNPY_AUTO_ALPHASIFT_LLM_MAX_RETRIES`. Invalid, incomplete, or timed-out ranking falls back immediately to deterministic `screen_score`. A cross-run circuit opens after the configured consecutive failures, skips LLM during cooldown, and permits exactly one shorter half-open recovery probe. Its five-minute lease blocks duplicate probes but converts an abandoned probe into a failure instead of locking recovery permanently; success closes the circuit and failure starts a new cooldown. Interactive screening is unaffected. Every Agent run persists policy and outcome in `diagnostics.alphasift_llm_policy` / `diagnostics.alphasift_llm_result` and projects them into the timeline.
- Automated buys now have optional persisted-snapshot market-light, market-breadth, and hotspot-retreat gates. Breadth compares the latest `MarketLightSnapshot` breadth score with a configured floor; hotspot retreat compares the latest two limit-dimension scores. When any gate is enabled, the latest snapshot must exist, load successfully, have a valid non-future date, and remain within the configurable 1-to-30-calendar-day freshness limit, which defaults to seven days. Enabled rules fail closed when required evidence is unavailable or stale, affect buys only, and persist the snapshots, age, thresholds, score delta, proxy definition, and reason in `diagnostics.market_context_risk` plus the Agent plan and timeline. They do not claim intraday breadth.
- Automated Agent runs now persist monotonic stage timings for planning, preflight gates, AlphaSift screening, candidate decision/execution, and total runtime. Successful, data-quality-blocked, and screening-failure paths retain the timing evidence, and run details expose it as a `performance` timeline event in the existing Agent console.
- AlphaSift candidate enrichment now enforces the declared three-candidate pre-rank limit in a request-scoped host callback, passes the already-fetched realtime quote into the fundamental valuation pipeline, and enriches at most three final candidates concurrently. Aggregation restores rank and warning order, removing the third-party default-five expansion, duplicate quote fetches, and serial news-search delay observed in timed production dry-runs.
- Candidate realtime quotes now share a manager-level provider recovery circuit across A-share and offshore routes. Three consecutive exceptions per market/provider open a five-minute cooldown, later sources continue serving requests, and one half-open probe determines recovery. Symbol-specific empty quotes do not poison provider health. Failure/open state is atomically persisted beside `DATABASE_PATH`, restored across API restarts for up to 24 hours, and never carries an old half-open lease; corrupt or stale state fails open. Agent diagnostics persist the state, failure count, remaining cooldown, and sanitized error under candidate-context source routing, and the console renders the actual route state.
- Candidate capital flow now has a genuinely independent `tushare_ths -> akshare` route when Tushare is configured. THS stock/industry fields are normalized from CNY 10K to CNY, the ten-day value is derived from the latest ten daily net amounts, and missing token/permission/data transparently falls back without failing screening. Exceptions use the same persisted three-failure/five-minute/single-probe recovery state as quotes. AlphaSift status, Agent run diagnostics, and the Web source-routing panel expose both providers. Provider state now lives in `provider_source_health.json`; the previous realtime-only file is read and migrated automatically.
- Candidate decisions now persist a versioned `strategy_evidence` contract in both decision and trade-plan order audits. It records the effective strategy, rank, screen/final scores, bounded upstream rule matches, factor decomposition, rationale, and LLM overlay without inferring missing matches. Evidence is `detailed`, `summary_only`, or `unavailable`; the Agent console renders the same state and remains compatible with legacy runs.
- Automated allocation now offers `target_tracking_min_variance_20d`: it builds a point-in-time annualized covariance matrix from overlapping local trailing returns, tracks arbitrary remaining position-target gaps (or candidate-score targets), applies a configurable variance penalty, and then passes the optimized weights through the existing cash, exposure, industry, order-slot, and lot constraints. Missing covariance history fails closed; Agent audits expose the target vector, matrix, convergence, penalty, and final weights.
- The consecutive-failure fuse remains latched, but it now supports an opt-in persisted cooldown. When enabled, the first open timestamp survives restarts, the due run is released as a single recovery probe, and another failed probe starts a fresh fuse cycle. Manual reset remains available, status exposes the recovery deadline, and automatic releases create an auditable `failure_fuse_auto_recovered` system event.

## Verified Local Status

Local API: `http://127.0.0.1:8000`

- Build `market-evidence-freshness-20260717` runs under Python 3.13.14 with launcher PID `14216` and listener PID `25424`. DSA_SIM is connected, the MainEngine bridge is available, and all four order/trade/account/position callbacks are registered.
- The status API returns both new gates disabled for the saved account, so this deployment does not change live order behavior until explicitly enabled. OpenAPI exposes `auto_intraday_market_gate_enabled` and `auto_cross_market_gate_enabled` plus their thresholds; `/paper-trading` and `/agent-console` both return HTTP 200.
- A headless Playwright check using the installed Chrome executable found exactly one visible instance of each new switch and threshold label at 1440x1000. The switch and threshold regions have no visible overlap or clipping; screenshots were retained only in the local temporary directory as review evidence and were not added to the repository.

- `GET /api/v1/vnpy-paper/status?include_snapshot=false&include_recent_trades=false`
  - `available=true`
  - `vnpy_available=true`
  - `diagnostics.vnpy_runtime.available=true`
  - `diagnostics.vnpy_runtime.gateway.added=true`
  - `diagnostics.vnpy_runtime.connect.connected=true`
  - `diagnostics.vnpy_runtime.event_bridge.registered=true`
  - `settings.auto_execution_mode=paper`
  - `scheduler.enabled=true`
  - `scheduler.loop_running=true`
  - both registered paper tasks report `overlap_guarded=true`
  - both registered paper tasks report `previous_generation_running=false` after restart
  - `settings.auto_trade_enabled=true`
  - `diagnostics.alphasift.available=true`
  - `diagnostics.alphasift.strategy_count=8`
  - `diagnostics.system_health.status=warning`
  - `diagnostics.system_health.next_action=next_session`
  - `diagnostics.system_health.required_blockers=[]`
  - `diagnostics.system_health.warnings=["next_session"]`
  - `diagnostics.system_health.components=paper_ledger,selection_source,automation_loop,scheduling_window,trading_window,valuation,industry_exposure,account_drawdown,consecutive_losses,vnpy_bridge`
  - `diagnostics.auto_trade_readiness.status=warning`
  - `diagnostics.auto_trade_readiness.next_action=next_session`
  - `diagnostics.auto_trade_readiness.timing_alignment.status=ready`
  - `diagnostics.auto_trade_readiness.timing_alignment.reason=next_auto_run_in_window`
  - next action: `next_session`
- `GET /api/v1/vnpy-paper/task-metrics?days=30`
  - route present in OpenAPI
  - `event_count=708`
  - `run_count=354`
  - `started_count=354`
  - `completed_count=353`
  - `skipped_count=1`
  - `failed_count=0`
  - `success_rate_pct=99.72`
  - `failure_rate_pct=0.0`
  - `skip_rate_pct=0.28`
  - `current_failure_streak=0`
  - `truncated=false`
  - 2 task series and 4 daily buckets
- `GET /api/v1/vnpy-paper/agent-runs/data-quality-trends?days=30`
  - `scanned_count=4`
  - `known_count=3`
  - `quality_counts={"partial":3,"unknown":1}`
  - `degraded_rate_pct=75.0`
  - `health=warning`
  - `truncated=false`
  - four legacy snapshot/daily source-health series are visible; candidate-context series will begin with runs created by the new build
- `GET /paper-trading` returned HTTP 200.
- `GET /api/v1/alphasift/status` reports `mode=circuit_breaker_failover` and `cross_process_persistence=true` for both candidate quote and fund-flow routing. The fund-flow priority is `tushare_ths,akshare`, and both routes report `restored_sources=0` for the clean current restart.

The latest post-change runtime uses listener PID `23180` (parent PID `11268`) and build id `local-news-provider-recovery-status-final`; Python 3.13.14, the vn.py runtime, built-in `DSA_SIM` gateway, MainEngine bridge, and all four EventEngine callbacks are available. The saved automatic execution mode remains `vnpy_paper`, matching the requested vn.py simulation path. Runtime connect diagnostics report `request_accepted=true`, `connected=true`, `status=connected`, and `confirmation_source=get_state_snapshot`; bridge diagnostics independently report a confirmed connection, and the required system-health component is ready. The opt-in reconnect monitor is running with base/current/maximum intervals of 5/5/300 seconds and zero consecutive failures. Fault injection remains at the gateway defaults (`reject_every_nth_order=0`, `duplicate_trade_event_count=1`) because no external settings file overrides them. Both paper background tasks remain overlap guarded. Automatic trading remains enabled and its next buy run is aligned to `2026-07-17T09:30:00` Asia/Shanghai. Agent run count remains eight and the latest run remains `ss-agent-20260716081103-27d104bb`; restarting after the news-provider recovery change created no production Agent run or order.

The readiness and system health status are `warning` because the current time is outside the A-share trading session and the next action is to wait for the next session. There are no required blockers; the scheduling alignment itself is ready.

## Recent Validation

- Python 3.13.14 vn.py acceptance regression
  - 276 tests passed across runtime bootstrap/reconnect, adapter mapping, DSA_SIM EventEngine behavior, paper service/API contracts, the online-Agent end-to-end gate, gateway soak, and scheduler soak. The nine gate tests include linked terminal coherence plus uncertain time-gate-disable and account-reset response recovery; py_compile, flake8, and `git diff --check` also passed.
- Live isolated-account positive-fill gate on `http://127.0.0.1:8000`
  - Run `ss-agent-20260720101013-dd345609` submitted and filled three plans in temporary account 3 with exact cash and position reconciliation. After the atomic plan/decision fix and API restart, run `ss-agent-20260720103034-40909a79` repeated the same 3/3 fill with matching decision terminal states. Independent reads confirmed current account 2, cash 17,702, nine original positions, both automation switches enabled, temporary accounts hidden, DSA_SIM connected, and four callbacks registered.
- Live online-Agent acceptance gate on `http://127.0.0.1:8000`
  - Dry-run `ss-agent-20260720095356-72a024b5` and order-authorized/all-existing-position run `ss-agent-20260720095531-de6c0400` each produced three traceable decisions and plans, zero failures, and no cash or position changes. The second run restored the time gate and ended with connected DSA_SIM and four callbacks. The earlier `ss-agent-20260720091729-9f5b1931` run remains the positive one-fill evidence.
- Python 3.13.14 read-only live market-metadata smoke
  - `DataFetcherManager.get_main_indices(region="cn")` returned all six configured A-share indices through the actual `akshare` fallback after efinance failed. Every row carried the same UTC `fetched_at`, `provider=akshare`, and `data_granularity=realtime`; `provider_timestamp` and `data_date` remained null rather than being invented. No Agent run or order was created.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 206 passed and 4 optional vn.py tests skipped. Coverage includes live CN index/breadth pass and fail-closed paths, linked-market weakness/missing quotes, VIX exclusion, provider timeout and worker-pool exhaustion, persisted settings, prior snapshot gates, sell-first behavior, and existing order lifecycle/recovery contracts.
- Combined provider, market-review, and paper-trading regression
  - 364 passed and 4 optional vn.py tests skipped across TickFlow/efinance/AkShare/Tushare/YFinance metadata, provider fallback, market-review compatibility, intraday end-of-day/session-date/provider-time rejection, closed-market session-bar linkage, and the complete paper service/API suites.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 61 focused Web tests passed. The settings API maps all five new fields and the page saves both switches and three thresholds. Lint reported zero errors and the pre-existing `SettingsPage.tsx:553` Hook warning; the production build passed.
- Read-only post-restart smoke on `http://127.0.0.1:8000`
  - Build `market-evidence-freshness-20260717` reports connected DSA_SIM, an available vn.py bridge, four registered callbacks, contract version 3, and no required health blockers. Latest Agent run remains `ss-agent-20260716081103-27d104bb` with zero submitted orders; the restart and UI smoke created no Agent run or order.

- `python -m pytest tests/test_vnpy_runtime.py tests/test_vnpy_adapter.py tests/test_vnpy_simulated_gateway.py tests/test_vnpy_paper_trading_api.py tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider`
  - 223 passed and 7 optional vn.py tests skipped under Python 3.14. Coverage includes gateway/runtime degradation, reconnect events, API contracts, native Chinese status normalization, immediate submission reconciliation, mixed-candidate run counts, missing-price audit, concurrent trade-callback idempotency, and existing order lifecycle/recovery behavior.
- Python 3.13.14 / vn.py 4.4.0 real-runtime fault validation
  - Nine direct unittest cases have passed across real MainEngine/EventEngine gateway events and DSA Agent-plan callbacks, including fill, reject, mixed-result, and missing-quote isolation cases. `scripts/check_vnpy_adapter.py --require-vnpy --fault-matrix --reconnect-cycles 3` reported one `ALLTRADED`, one `REJECTED` with `simulated_configured_rejection`, two callbacks carrying one unique trade id, one deduplicated MainEngine trade, and three reconnect cycles with every pending order retained and filled exactly once.
- Python 3.13.14 / vn.py 4.4.0 mixed-candidate Agent validation
  - One real MainEngine/EventEngine run submitted `600519`, `000001`, and `300750`; the configured gateway produced `filled / failed / filled`, the rejected middle order retained `simulated_configured_rejection`, duplicate callbacks produced exactly two unique Portfolio trades, persisted run counts ended at two submitted and one skipped, and no active gateway order remained.
- Python 3.13.14 / vn.py 4.4.0 missing-quote isolation validation
  - One real MainEngine/EventEngine run evaluated `600519`, `000001`, and `300750`; the middle candidate had no supplied price and its realtime quote source raised a configured failure. Persisted results ended `filled / skipped / filled`, the skipped plan retained `price_resolution.source=unavailable`, only two orders reached the gateway, exactly two Portfolio trades were recorded, and no active gateway order remained.
- `scripts/check_vnpy_gateway_soak.py` no-order acceptance validation
  - A deliberate 8-second DSA_SIM run disconnected at second 1 and recovered through the five-second runtime monitor without manual reconnect. It recorded 16 samples, transitions `connected -> disconnected -> connected`, one successful reconnect, two account events, no order/trade events, and a 43.75% connected ratio above the explicit 40% short-fault threshold. A separate negative run requiring a position event against the empty account exited nonzero with `required_events_missing`, proving that missing-event gates are enforced.
- Isolated API-lifespan reconnect-event smoke with a temporary database
  - Starting with zero `vnpy_paper` alert events and background reconnect disabled, `POST /api/v1/vnpy-paper/gateway/reconnect` returned `attempted=true`, `connected=true`, and `result=reconnected`. The asynchronous sink persisted one `resolved` event with source `vnpy_paper_auto`, reason `vnpy_gateway_reconnected`, gateway `DSA_SIM`, and trigger `manual`. Its 258-character diagnostics value remained valid JSON and did not contain the connection settings path.
- Isolated Python 3.13 `DsaSimulatedGateway` reconnect smoke
  - With background auto reconnect disabled, a previously confirmed gateway was closed and recovered by one manual reconnect call. The result reported `last_trigger=manual`, one attempt, one success, `last_result=reconnected`, and renewed `get_state_snapshot` confirmation. The online API no-op check against an already connected gateway reported `attempted=false` and left Agent run count unchanged at eight.
- `.venv-vnpy\Scripts\python.exe scripts\check_vnpy_adapter.py --require-vnpy --reconnect-cycles 3`
  - Python 3.13.14 and vn.py 4.4.0 passed. Three in-flight disconnect/reconnect cycles retained every pending order, filled every order exactly once, kept order/trade ids unique, and ended connected with four orders/trades and the expected CNY 996,000 balance.
- Read-only runtime smoke on `http://127.0.0.1:8000`
  - Build `local-vnpy-missing-quote-final` reports execution mode `vnpy_paper`, connected `DSA_SIM` confirmed by `get_state_snapshot`, four EventEngine callbacks, and a running reconnect monitor with 5/5/300-second base/current/maximum intervals. Agent run count/latest UID stayed 8 / `ss-agent-20260716081103-27d104bb`, and the automatic buy task remains aligned to `2026-07-17T09:30:00` Asia/Shanghai.
- `npm.cmd run test -- --run src/pages/__tests__/VnpyPaperTradingPage.test.tsx -t reconnect`, `npm.cmd run lint`, and `npm.cmd run build`
  - The reconnect interaction passed and verifies that alert history refreshes after the command. Lint completed with zero errors and the existing `SettingsPage.tsx:553` Hook warning; the TypeScript/Vite production build passed.
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts` and the focused manual-reconnect page test
  - 34 Web API tests and the operator reconnect interaction passed. The full two-file run also passed all new tests; one unrelated existing manual-order page test hit its 10-second suite timeout under combined load and passed alone in 6.52 seconds. Lint completed with zero errors and the existing `SettingsPage.tsx:553` hook warning; the production build passed.
- `python -m pytest tests/test_runtime_scheduler_service.py tests/test_scheduler_background.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py tests/test_runtime_scheduler_repo.py -q -p no:cacheprovider`
  - 216 passed and 1 optional test skipped. Coverage includes cross-generation task overlap after reconcile, persisted `task_already_running` audit, replacement-generation running visibility, exception-path lock release, and the existing scheduler, paper-trading service/API, and task-event repository paths.
- `npm.cmd run test -- --run src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/api/__tests__/vnpyPaperTrading.test.ts`, `npm.cmd run lint`, and `npm.cmd run build`
  - 59 focused Web tests passed; lint completed with zero errors and one pre-existing `SettingsPage.tsx` Hook dependency warning; the production build passed.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py tests/test_alphasift_api.py tests/test_runtime_scheduler_service.py -q -p no:cacheprovider`
  - 308 passed and 1 optional test skipped. The service-only rerun passed 143 tests with 1 optional skip. Coverage includes detailed and summary-only strategy evidence, bounded rule/factor normalization, non-finite factor removal, mirrored decision/trade-plan audits, legacy API behavior, and existing AlphaSift/scheduler/trading paths.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 15 Agent-console tests passed and the production build completed. The candidate table renders strategy status, rank, screen score, rule matches, boolean match values, and factor decomposition. Lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only strategy-evidence compatibility smoke on `http://127.0.0.1:8000`
  - Build `local-strategy-evidence-final` runs under Python 3.13.14 with connected `DSA_SIM` and four EventEngine callbacks. The latest legacy run has no `strategy_evidence` but its detail endpoint, `/paper-trading`, and `/agent-console` all returned HTTP 200. Execution remains `paper`, automatic trading remains enabled and aligned to `2026-07-17T09:30:00`, and Agent run count/latest UID stayed 8 / `ss-agent-20260716081103-27d104bb`.
- `python -m pytest tests/test_alphasift_api.py tests/test_fundamental_context.py tests/test_tushare_fetcher_followups.py tests/test_realtime_source_health_persistence.py tests/test_realtime_types.py tests/test_fetcher_source_optimization.py tests/test_runtime_scheduler_service.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py tests/test_data_tools_get_capital_flow.py tests/test_decision_stability.py -q -p no:cacheprovider`
  - 387 passed and 1 optional test skipped. Coverage includes Tushare THS field/unit normalization, Tushare-to-AKShare fallback, provider circuit opening and half-open recovery, restart persistence and legacy-state migration, AlphaSift route projection, Agent-run audit persistence, and existing paper-trading behavior.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 15 Agent-console tests passed and the production build completed. Lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only fund-flow routing runtime smoke on `http://127.0.0.1:8000`
  - Build `local-fund-flow-routing-final` runs under Python 3.13.14 with connected `DSA_SIM` and four EventEngine callbacks. AlphaSift status reports persisted quote and fund-flow recovery policies with fund-flow priority `tushare_ths,akshare`; automatic trading remains in `paper` mode and aligned to `2026-07-17T09:30:00`. Both Web pages returned HTTP 200, and Agent run count/latest UID remained 8 / `ss-agent-20260716081103-27d104bb`.
- `python -m pytest tests/test_realtime_types.py tests/test_realtime_source_health_persistence.py tests/test_fetcher_source_optimization.py tests/test_runtime_scheduler_service.py tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 335 passed and 1 optional test skipped before the final status projection test; the final focused status/recovery suite passed 158 tests. Coverage includes restart reconstruction, half-open lease normalization, expired recovery probing, corrupt/stale state fail-open behavior, healthy-request write suppression, API lifespan compatibility, AlphaSift status projection, and existing paper-trading behavior.
- Read-only provider-health persistence runtime smoke on `http://127.0.0.1:8000`
  - Build `local-provider-health-persistence-final` runs under Python 3.13.14 with connected `DSA_SIM` and four EventEngine callbacks. AlphaSift status reports cross-process quote-health persistence enabled with a clean zero-source recovery; automatic trading remains enabled and aligned to `2026-07-17T09:30:00`. Both Web pages returned HTTP 200, and Agent run count/latest UID remained 8 / `ss-agent-20260716081103-27d104bb`.
- `python -m pytest tests/test_realtime_types.py tests/test_fetcher_source_optimization.py tests/test_hk_realtime_routing.py tests/test_realtime_quote_fallback_logging.py tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py -q`
  - 279 passed and 1 optional test skipped, covering exception-triggered failover, open-circuit source skipping, symbol-specific empty-result isolation, single half-open recovery, non-consuming diagnostics, A/HK/US routing compatibility, AlphaSift route projection, Agent-run persistence, and existing trading behavior.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`
  - 15 tests passed, including realtime provider state, failure-count, and cooldown rendering in the existing source-routing panel.
- Read-only candidate-quote recovery runtime smoke on `http://127.0.0.1:8000`
  - Build `local-candidate-quote-recovery-final` runs under Python 3.13.14 with connected `DSA_SIM`, an available MainEngine, and four EventEngine callbacks. Automatic trading remains enabled and aligned to the next A-share session at `2026-07-17T09:30:00`; `/paper-trading` and `/agent-console` returned HTTP 200. Agent run count stayed at 8 and the latest run UID was unchanged.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 178 passed and 1 optional test skipped, including market-breadth blocking, hotspot-retreat two-snapshot comparison, missing/invalid/future/stale evidence fail-closed behavior, service-restart setting persistence, Agent plan actions, decision/trade-plan reasons, timeline diagnostics, and existing paper-trading behavior.
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 74 tests passed, including settings mapping, form controls, save payloads, and market-context timeline rendering. The production build passed; lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only market-context gate runtime smoke on `http://127.0.0.1:8000`
  - Build `local-market-context-gates-final` runs under Python 3.13.14 with connected `DSA_SIM`, an available MainEngine bridge, and four registered EventEngine callbacks. Both new gates are loaded disabled with defaults 35/25; `/paper-trading` and `/agent-console` returned HTTP 200. Agent run count remained 8 and the latest run UID was unchanged.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 275 passed and 1 optional test skipped, including cross-run ranking failure detection, open-circuit fallback, single half-open recovery, concurrent-probe suppression, abandoned-probe lease expiry, disabled-circuit diagnostics, and existing AlphaSift/API/trading behavior.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 15 Agent-console tests passed and the production build completed. Lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only AlphaSift LLM circuit runtime smoke on `http://127.0.0.1:8000`
  - Build `local-alphasift-llm-circuit-final-v2` runs under Python 3.13.14 with connected `DSA_SIM`, an available MainEngine bridge, and four registered EventEngine callbacks. The current policy is `open/circuit_open`, uses deterministic `screen_score`, and records a 300-second recovery-probe lease. `/paper-trading` and `/agent-console` returned HTTP 200; Agent run count remained 8 and the latest run UID was unchanged.
- Three production-runtime `dry_run` measurements on Python 3.13.14 with connected `DSA_SIM`
  - All three runs completed with three candidates and zero planned/submitted orders. The first timing baseline was 135.36 seconds total / 127.91 seconds AlphaSift. Reusing quotes plus bounded final-candidate enrichment reduced this to 110.59 / 101.62 seconds. Enforcing the declared three-candidate pre-rank limit reduced the final run to 91.91 / 83.99 seconds, a 32.1% total and 34.3% AlphaSift reduction from baseline. Logs between the final snapshot and LLM marker contain exactly three candidate quote calls. The remaining dominant delay is the bounded 45-second LLM timeout.
- `python -m pytest tests/test_alphasift_api.py tests/test_fundamental_context.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 293 passed and 1 optional test skipped, covering stage timings, timeline projection, request-scoped candidate limits, supplied-quote valuation reuse, bounded concurrent enrichment, stable result/warning order, and existing AlphaSift/API/trading behavior.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 266 passed and 1 optional test skipped after adding persisted Agent stage timings and `performance` timeline events across successful, data-quality-blocked, and AlphaSift-failure paths.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 266 passed and 1 optional test skipped, including request-scoped AlphaSift LLM budgets, environment restoration, configurable automated defaults, persisted policy diagnostics, and existing AlphaSift/API/trading behavior.
- Scheduler-suppressed runtime smoke on `http://127.0.0.1:8010`
  - Build `local-auto-llm-budget-smoke` returned health `ok` and HTTP 200 for the paper-trading and Agent pages, kept the quality floor at 60, and confirmed the scheduler loop was disabled. The temporary Python 3.14 process was stopped after validation; the existing Python 3.13 vn.py service on port 8000 was not interrupted during the trading session.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 264 passed and 1 optional test skipped, including the default 60 quality floor, legacy null normalization, explicit zero audit-only mode, hard stale/unavailable precedence, and existing AlphaSift/API/trading behavior.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx`
  - 59 passed, including the paper-trading settings default and API mapping.
- `npm.cmd run lint` and `npm.cmd run build`
  - Production build passed; lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only default-quality runtime smoke on `http://127.0.0.1:8000`
  - Build `local-default-quality-gate-final` exposes `auto_min_data_quality_score=60.0`, connected `DSA_SIM`, four event callbacks, and HTTP 200 for the paper-trading and Agent pages; database counts remained `5/9/12`.
- `python -m pytest tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider`
  - 124 passed and 1 optional test skipped, including fail-closed missing trading status, unavailable configured turnover, and existing auto-trading behavior.
- Persisted-candidate field-gate replay under Python 3.13.14
  - The actual filled `600016` candidate from run `ss-agent-20260716050026-2b3951b2` had `missing_fields=["trading_status"]`; the new build resolves it to `candidate_trading_status_unavailable` without creating a run or order.
- Live scheduled context-health observation
  - Run `ss-agent-20260716050026-2b3951b2` completed with quote `3/3`, news `3/3`, and fund-flow `0/3` source success; its 46.16 quality score and persisted errors are visible in run diagnostics and cross-run source trends.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 260 passed and 1 optional test skipped, including healthy/degraded candidate-context source aggregation, quality-score impact, cross-run source trend API output, and existing AlphaSift/trading behavior.
- `python -m flake8 src/services/alphasift_service.py tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py --select=E9,F63,F7,F82`
  - Passed with no critical Python errors.
- Read-only runtime smoke on `http://127.0.0.1:8000`
  - Build `local-context-source-health-final` loaded with connected `DSA_SIM`, four vn.py event callbacks, and HTTP 200 for `/paper-trading` and `/agent-console`; Agent-run, trade-plan, and Portfolio-trade counts remained `4/6/11`.
- `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 166 passed and 1 optional test skipped, including final-review de-duplication, low passed-precision blocking, low blocked-avoidance blocking, insufficient-evidence behavior, dynamic-objective tightening, and existing trading/API paths.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 73 passed, including effective-review policy mapping and current calibration evidence in the Agent console.
- `npm.cmd run lint` and `npm.cmd run build`
  - Production build passed; lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only cross-run calibration smoke on `http://127.0.0.1:8000`
  - Three historical review samples remained `insufficient_evidence` with zero mature passed/blocked 5-day samples and `gate_blocked=false`; run count and latest run were unchanged, with no order submitted.
- `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 165 passed and 1 optional test skipped, including real `action=skip` inclusion, per-reviewer/model/version grouping, passed precision, blocked-loss avoidance, return spread, and existing trading/API behavior.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 73 passed, including snake-case mapping and the dynamic long-horizon review-quality matrix.
- `npm.cmd run lint` and `npm.cmd run build`
  - Production build passed; lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only forward-evaluation smoke on `http://127.0.0.1:8000`
  - Three historical candidates produced one real rule-review quality group under build `local-review-quality-matrix-final`; run count and latest run UID were unchanged, with no order submitted.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 158 passed and 1 optional test skipped, including execution-effective HK tightening, rejected US feedback, monotonic guardrails against looser overrides, and existing API/trading behavior.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 73 passed, including the cross-market objective configured/effective values and trigger reasons in the Agent console.
- `npm.cmd run lint` and `npm.cmd run build`
  - Production build passed; lint reported zero errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Read-only runtime smoke on `http://127.0.0.1:8000`
  - Build `local-cross-market-objective-final` runs under Python 3.13.14 with connected `DSA_SIM`, an available MainEngine bridge, four registered EventEngine callbacks, and Agent console HTTP 200. No Agent run or order was created.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 156 passed and 1 optional test skipped, covering human-feedback upsert, details/list/timeline/daily summary output, recent-run context reuse, and existing trading behavior.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`
  - 47 passed, covering feedback snake-case mapping, verdict/reviewer/note saving, detail refresh, and existing Agent-console interactions.
- `npm.cmd run lint` and `npm.cmd run build`
  - Production build passed; lint reported no errors and retained the pre-existing `SettingsPage.tsx:553` hook dependency warning.
- Live feedback smoke on `http://127.0.0.1:8000`
  - OpenAPI exposed `PUT /api/v1/vnpy-paper/agent-runs/{run_uid}/feedback`; a temporary run returned `accepted=true`, one `human_feedback` timeline event, and `human_feedback_reviewed_count=1`. The temporary run was removed after verification and no order was submitted.
  - In-app visual inspection could not initialize because the existing browser runtime raised `Cannot redefine property: process`; component interaction tests, production build, and live HTTP evidence are used instead.

- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 147 passed and 1 optional test skipped after adding arbitrary position-target tracking, point-in-time covariance optimization, adaptive projected-gradient convergence, missing-history fail-closed behavior, and settings/API audit coverage.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 67 tests passed; lint completed with zero errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript and Vite production build passed.
- `python -m pytest tests/test_alphasift_api.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 244 passed and 1 optional test skipped after adding complete source-health capture, bounded 30-day dynamic snapshot routing, explicit-priority protection, Agent audit propagation, and cache-fixture time stability.
- `npm.cmd test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`, `npm.cmd run lint`, and `npm.cmd run build`
  - 13 Agent-console tests passed; lint completed with zero errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript and Vite production build passed.
- `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 150 passed and 1 optional test skipped after adding the read-only current-quality endpoint and prefixed/suffixed daily-code resolution to cross-run forward-quality states plus correlation-capped risk allocation.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 67 passed after adding cross-run configuration mapping, settings controls, current-quality loading, and Agent state/transition rendering.
- `npm.cmd run lint` and `npm.cmd run build`
  - Lint completed with zero errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript and Vite production build passed.
- `python -m pytest tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider -k "data_quality_score or auto_trade_records_audit"`
  - 2 focused quality-score and audit tests passed; the full vn.py paper suite is rerun below after final integration.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 139 passed and 1 optional test skipped after adding point-in-time inverse-volatility allocation, unified data-quality scoring/gating, missing-risk fail-closed behavior, volatility-floor protection, API persistence, alert auditing, and constrained redistribution coverage.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 66 passed after adding allocation-method settings and Agent risk-input audit rendering.
- `npm.cmd run lint` and `npm.cmd run build`
  - Lint completed with zero errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript and Vite production build passed.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 133 passed and 1 optional test skipped after adding score-weighted constrained portfolio allocation, lot residual audit, settings/API contracts, and Agent position-plan coverage.
- `npm.cmd test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 66 passed; `npm.cmd run lint` completed with zero errors and the pre-existing `SettingsPage.tsx:553` warning, and `npm.cmd run build` passed.
- `python -m flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=.git,.tools,.venv-vnpy,.uv-cache-vnpy,.vntrader,node_modules,static`
  - 0 critical findings in repository-controlled source. The unmodified `scripts/ci_gate.sh` cannot complete locally because `flake8 .` and pytest collect ignored `.tools` runtimes; its deterministic shell check also hits a Windows GBK emoji encoding error. The broad local offline run still completed 4551 passes and 3 skips with 31 failures outside the changed vn.py paths, while the focused suite above is green.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 119 passed and 1 optional test skipped before the final strict-industry health assertion was added; the final focused service run passed 91 tests with 1 optional skip.
- `npm.cmd run test -- --run src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/api/__tests__/vnpyPaperTrading.test.ts`
  - 53 passed; the final page-only run passed 23 tests after the non-blocking snapshot-only display wording was refined.
- `npm.cmd run lint` and `npm.cmd run build`
  - Both passed; lint retains the pre-existing `SettingsPage.tsx:553` exhaustive-deps warning.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 123 passed and 1 optional test skipped after adding persisted observed-equity-peak drawdown and system-health coverage.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx src/api/__tests__/vnpyPaperTrading.test.ts`
  - 43 passed after adding per-source health trend mapping and display coverage.
- `python -m pytest tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider`
  - 93 passed and 1 optional test skipped after adding deterministic recent-run context and dynamic-plan v2 prompt reuse.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`
  - 13 passed with recent-run context display coverage.

- `python -m py_compile api\v1\endpoints\vnpy_paper_trading.py api\v1\schemas\vnpy_paper_trading.py src\repositories\runtime_scheduler_repo.py src\services\runtime_scheduler.py`
- `python -m pytest tests/test_runtime_scheduler_repo.py tests/test_runtime_scheduler_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 50 passed
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx`
  - 51 passed
- `python -m pytest tests/test_vnpy_paper_trading_api.py tests/test_vnpy_paper_trading_service.py -q`
  - 95 passed
- `python -m pytest tests/test_vnpy_adapter.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`
  - 117 passed
  - Covers MainEngine order/trade snapshots, cumulative multi-fill callbacks, partial-fill cancellation summaries, proactive missed-callback reconciliation, paused-buy recovery, cancel races, late fills, query-failure protection, and recovery API counters.
- `python -m pytest tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider`
  - 80 passed.
  - Covers service reconstruction recovery, pre-timeout no-evidence waiting, the 60-second grace period, paused-buy recovery scheduling, cancel-request fill races, and post-timeout late fills.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_runtime_scheduler_service.py tests/test_vnpy_paper_trading_api.py tests/test_vnpy_runtime.py tests/test_vnpy_adapter.py tests/test_vnpy_simulated_gateway.py -q -p no:cacheprovider`
  - 148 passed and 2 optional-runtime tests skipped under system Python 3.14.
  - Covers API lifespan runtime binding, scheduler dependency forwarding, immediate recovery registration, runtime bootstrap, bridge adapter, built-in gateway contracts, task health, and order lifecycle recovery. The two skipped tests passed separately under Python 3.13 with vn.py installed.
- `.venv-vnpy\Scripts\python.exe scripts\check_vnpy_adapter.py --require-vnpy`
  - Python 3.13.14, vn.py 4.4.0, real `vnpy.trader.object.OrderRequest`, real `vnpy.event.engine.EventEngine`, and real `vnpy.trader.engine.MainEngine` all passed.
  - Bridge smoke returned `accepted=true`; the built-in `DsaSimulatedGateway` returned `vt_orderid=DSA_SIM.1`, `order_status=ALLTRADED`, and one real vn.py trade event; `pip check` reported no broken requirements.
- `.venv-vnpy\Scripts\python.exe scripts\check_vnpy_adapter.py --require-vnpy --reconnect-cycles 3`
  - Three in-flight disconnect/reconnect cycles completed with `all_pending_retained=true` and `all_filled_once=true`; all order and trade ids were unique, and the final simulator state retained four orders/trades, one position, and the expected `996000.0` balance across four connections.
- `.venv-vnpy\Scripts\python.exe -m unittest` built-in gateway integration smoke
  - A fixed AlphaSift candidate produced an Agent `submitted` plan through real MainEngine, then EventEngine callbacks changed the plan and decision to `filled` and wrote one Portfolio trade.
  - A direct gateway test also verified order, trade, account, and position snapshots through vn.py's OMS engine.
- Isolated API process smoke on `http://127.0.0.1:8011` with `VNPY_RUNTIME_ENABLED=true`
  - `diagnostics.vnpy_runtime.available=true`, `event_bridge.registered=true`, and four vn.py event types were attached.
  - MainEngine exposed send, cancel, order-query, and trade-query capabilities, and the runtime scheduler loop was active.
  - Recovery-task registration is covered by the focused scheduler/API regression suite; the isolated status response was not used to assert a task-list entry.
  - `diagnostics.vnpy_bridge.available=false` remained expected because no gateway name or gateway plugin was configured. The temporary process and port were stopped after validation.
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx`
  - 52 passed, including recovery counter mapping and the Web recovery result summary.
- Current local API process on `http://127.0.0.1:8000`
  - The API is running from the Python 3.13 vn.py environment with `runtime_available=true`, `gateway_added=true`, `gateway_connected=true`, and `event_bridge_registered=true`.
  - Settings inherit `gateway_name=DSA_SIM`; `diagnostics.vnpy_bridge.available=true` and the runtime scheduler loop is active.
  - The saved execution mode remains `paper`, so startup does not silently redirect scheduled orders. Selecting `vnpy_paper` in the Web settings explicitly activates the built-in gateway route.
- `python -m pytest tests -m "not network" -q --basetemp <workspace-temp> -p no:cacheprovider`
  - 4077 passed, 15 failed, 187 errors, 4 deselected.
  - The remaining suite failures are outside the changed vn.py paths and cluster around existing global configuration isolation, Windows subprocess handling, static bundle fixtures, and temporary-directory behavior. The focused vn.py suite is green with 148 passed and 2 optional-runtime skips under system Python.
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/VnpyPaperTradingPage.test.tsx src/pages/__tests__/AgentConsolePage.test.tsx`
  - 59 passed
- Browser verification at 1440x900 and 390x844
  - Agent quality-trend view rendered with live API data
  - no page-level horizontal overflow after constraining run history/detail cards
- `npm.cmd run lint -- --max-warnings=9999`
  - Passed with one existing warning in `apps/dsa-web/src/pages/SettingsPage.tsx`.
- `npm.cmd run build`
  - Passed.
- `git diff --check`
  - No whitespace errors; only Windows LF/CRLF warnings.
- `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_runtime_scheduler_service.py tests/test_vnpy_paper_trading_api.py tests/test_vnpy_runtime.py tests/test_vnpy_adapter.py tests/test_vnpy_simulated_gateway.py tests/test_portfolio_pr2.py -q -p no:cacheprovider`
  - 181 passed and 2 optional-runtime tests skipped under system Python 3.14.
  - Covers base/quote currency order audit, stale/missing FX fail-closed buys, cross-currency daily budget and exposure calculations, sell-side risk reduction, scheduler integration, and vn.py recovery paths.
- `python -m pytest tests/test_data_tools_portfolio_snapshot.py tests/test_portfolio_alerts.py tests/test_portfolio_api.py tests/test_portfolio_pr2.py tests/test_portfolio_service.py -q -p no:cacheprovider`
  - 93 passed across the shared Portfolio service and API surface.
- `.venv-vnpy\Scripts\python.exe -m unittest` built-in gateway integration smoke after the FX changes
  - 2 passed with real vn.py 4.4.0 MainEngine/EventEngine and the built-in `DSA_SIM` gateway.
- `.venv-vnpy\Scripts\python.exe -m pip check`
  - No broken requirements found.
- `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_backtest_engine.py tests/test_backtest_summary.py tests/test_backtest_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`
  - 115 passed.
  - Covers strict post-decision daily-bar selection, multi-horizon metrics, strategy/date filters, skipped-candidate cohorts, missing-data coverage, the existing backtest engine/service, and the API contract.
- `npm.cmd run test -- --run src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`
  - 38 passed, including the Agent forward-evaluation request mapping and matrix rendering.
- `npm.cmd run lint` and `npm.cmd run build`
  - Passed; lint retains one pre-existing React hook dependency warning in `SettingsPage.tsx`.
- Live `POST /api/v1/vnpy-paper/agent-runs/backtest` smoke with local data
  - OpenAPI route present, 3 persisted candidates scanned, and 1/5/10/20-day coverage all returned `0.0%` with `insufficient_forward_bars=3`, as expected before any later trading-day bars exist.
  - `lookahead_protection=true`, `reruns_historical_strategy=false`, `/agent-console` returned HTTP 200, and the saved execution mode remained `paper`.
- `python -m pytest tests/test_alphasift_replay_api.py tests/test_stock_selection_strategy_replay_service.py tests/test_stock_selection_agent_backtest_service.py -q -p no:cacheprovider`
  - 12 passed, covering factor-snapshot idempotency, date isolation, missing-field fail-closed behavior, deterministic AlphaSift ranking, replay API contracts, and the existing persisted-candidate forward evaluation.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx src/api/__tests__/alphasift.test.ts`
  - 21 passed, covering replay API request mapping, factor-coverage rendering, missing-field diagnostics, and candidate-table output.
- `npm.cmd run lint` and `npm.cmd run build`
  - Passed; lint retains one pre-existing React hook dependency warning in `SettingsPage.tsx`.
- Broader `tests/test_alphasift_api.py` run
  - 108 passed and 2 existing date-sensitive cache tests failed because their fixed `cached_at=2026-07-03` fixture is beyond the configured cache age on 2026-07-14; the new replay tests are unaffected.
- Live point-in-time replay smoke on `http://127.0.0.1:8000`
  - OpenAPI includes `/api/v1/alphasift/replay/snapshots`, `/replay/compatibility`, and `/replay/run`; an empty 2024-01-05 snapshot returned zero hard/score coverage plus the required-field lists, `/agent-console` returned HTTP 200, and no replay or trading order was submitted.
- `python -m pytest tests/test_stock_selection_factor_ingestion_service.py tests/test_stock_selection_portfolio_backtest_service.py tests/test_stock_selection_strategy_replay_service.py tests/test_stock_selection_agent_backtest_service.py tests/test_alphasift_replay_api.py -q -p no:cacheprovider`
  - 24 passed, covering valuation as-of protection, ingestion degradation, portfolio entry/exit timing, costs on actual entries/exits, overlap retention, aggregate and per-period turnover, suspension/price-limit trade gates, blocked-exit carry and later liquidation, benchmark/excess return, missing-bar coverage, replay gates, and background-task API contracts.
- `npm.cmd run test -- --run src/pages/__tests__/AgentConsolePage.test.tsx src/api/__tests__/alphasift.test.ts`
  - 30 passed after adding historical ingestion, portfolio-backtest, blocked-position audit, historical-universe preview, and persistent full-market job create/list/resume API/UI workflows.
- `python -m pytest tests/test_stock_selection_historical_universe_service.py tests/test_tushare_fetcher_get_stock_list.py tests/test_stock_selection_factor_ingestion_service.py tests/test_stock_selection_portfolio_backtest_service.py tests/test_stock_selection_strategy_replay_service.py tests/test_stock_selection_agent_backtest_service.py tests/test_alphasift_replay_api.py -q -p no:cacheprovider`
  - 45 passed across Tushare lifecycle aggregation, dated-universe filtering, bounded factor ingestion, replay, portfolio behavior, forward evaluation, and API contracts.
- `python -m pytest tests/test_stock_selection_historical_universe_service.py tests/test_tushare_fetcher_get_stock_list.py tests/test_stock_selection_factor_ingestion_service.py tests/test_stock_selection_full_market_ingestion_service.py tests/test_stock_selection_portfolio_backtest_service.py tests/test_stock_selection_strategy_replay_service.py tests/test_stock_selection_agent_backtest_service.py tests/test_alphasift_replay_api.py -q -p no:cacheprovider`
  - 61 passed, adding per-date universe isolation, persistent batch checkpoints, failed-batch resume, force-takeover lease protection, recent-job recovery, cash/whole-lot accounting, explicit target weights, residual cash, configured-but-unselected audit, minimum commission, sell-only tax, cost reconciliation, cash dividends, split adjustments, same-day entitlement ordering, fractional-share rejection, drift rebalancing, blocked final liquidation, and full-market/API contracts to the prior replay coverage.
- `python -m pytest tests/test_stock_selection_full_market_ingestion_service.py tests/test_stock_selection_factor_ingestion_service.py tests/test_stock_selection_corporate_action_repo.py tests/test_stock_selection_portfolio_backtest_service.py tests/test_alphasift_replay_api.py tests/test_tushare_fetcher_get_stock_list.py -q`
  - 53 passed, covering implemented-dividend normalization, idempotent corporate-action storage, source-failure degradation, resumable-job event totals, persisted-event replay, explicit-event precedence, and API contracts.
- `python -m pytest tests/test_stock_selection_full_market_ingestion_service.py tests/test_stock_selection_factor_ingestion_service.py tests/test_stock_selection_corporate_action_repo.py tests/test_stock_selection_portfolio_backtest_service.py tests/test_alphasift_replay_api.py tests/test_tushare_fetcher_get_stock_list.py -q`
  - 55 passed after adding explicit fractional-share cash-in-lieu settlement plus official-date A-share stamp-duty resolution, per-trade tax source audit, unsupported market/date rejection, and API forwarding to the broader ingestion/replay suite.
- `cd apps/dsa-web && npm.cmd test -- --run src/api/__tests__/alphasift.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`
  - 31 passed, covering cash-in-lieu request mapping, snake-case JSON parsing, invalid cash-dividend field rejection, event audit rendering, and aggregate settlement display.
- `cd apps/dsa-web && npm.cmd test -- --run src/api/__tests__/alphasift.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`
  - 30 passed across the AlphaSift API mapping and Agent-console interaction paths changed by explicit portfolio target weights.
- `cd apps/dsa-web && npm.cmd run lint && npm.cmd run build`
  - Lint completed with the pre-existing `SettingsPage.tsx:553` exhaustive-deps warning and no errors; the production TypeScript/Vite build passed.
- Full Web test observation
  - 969 passed, 2 skipped, and 3 unrelated tests failed in `AlertRuleForm`, `SettingsPage`, and `VnpyPaperTradingPage`. The changed AlphaSift/Agent-console suites passed; the unrelated failures were not modified in this scoped change.
- UI screenshot verification
  - The in-app Browser runtime still fails during initialization with `Cannot redefine property: process`, so no new screenshot could be captured. Component interaction tests, the production build, and live HTTP/OpenAPI smoke are used as replacement evidence; standalone Playwright was not substituted.
- Final live replay/backtest smoke on `http://127.0.0.1:8000`
  - OpenAPI exposes replay paths for historical-universe resolution, persistent full-market ingestion create/list/detail/resume, bounded ingestion status, and portfolio backtest. The portfolio request schema includes `target_weights`, `minimum_commission`, `sell_tax_bps`, and up to 500 typed `corporate_actions`; action types are restricted to cash dividends and split adjustments. The two cost fields default to zero, while `accounting_mode` defaults to `cash_ledger` and `enforce_tradeability` to `true`. `/agent-console` returned HTTP 200; the isolated Python 3.13 vn.py runtime, simulated gateway connection, event bridge, and scheduler loop remained available under server PID `17784`. Execution mode remained `paper`; recovery reported zero attempted/submitted orders and no order was triggered by the smoke.
  - The earlier HTTP 424 caused by missing Tushare `stock_basic` permission is superseded by the complete BaoStock+BSE official fallback. Live requests now return 5,338 members for 2024-01-05 and 5,529 for 2026-07-17 with explicit SSE/SZSE/BSE coverage, no truncation, no current-universe fallback, and a zero-gap BSE coverage audit.
  - A first external job exposed 100% EastMoney daily-history proxy failures and was deliberately orphaned after 150 checkpoints. The daily route was replaced with audited provider fallback. Clean replacement job `bfedb234376d42f1a04490d3730c52e7` then completed all 5,529 work items in 111 batches: 5,494 complete factor rows (99.367%), 5,522 exact-date daily rows (99.8734%), 5,502 valuation-complete rows (99.5117%), and 27 audited source errors (0.4883%). The acceptance gate returned `ok=true` with no failures and `recovery_state=complete`.
- Corporate-action persistence smoke after restart on `http://127.0.0.1:8000`
  - Startup created `stock_selection_corporate_actions`; OpenAPI exposes `include_persisted_corporate_actions=true` by default, `/agent-console` returned HTTP 200, and the `DSA_SIM` vn.py runtime reported a connected gateway and registered event bridge under listener PID `488` (launcher PID `15956`). This read-only smoke submitted no order. The account retained pre-existing trade and active-order history.
  - The external Tushare `dividend` permission is still unavailable, but it is no longer a blocker for implemented SSE/SZSE events. The production default route fell back to AkShare EastMoney and persisted three verified `600519` cash-dividend rows with zero ingestion errors. BSE free-source corporate-action coverage remains unavailable and is explicitly reported rather than inferred.
- Fractional-share settlement smoke after restart on `http://127.0.0.1:8000`
  - OpenAPI exposes nullable-positive `cash_in_lieu_price` on split-adjustment events, `/agent-console` returned HTTP 200, and `DSA_SIM` remained connected with the event bridge registered under listener PID `27140` (launcher PID `20776`). This read-only smoke submitted no order.
- Historical stamp-duty smoke after restart on `http://127.0.0.1:8000`
  - OpenAPI exposes `sell_tax_mode` with `explicit` default and `explicit|cn_historical_stamp_duty` validation, `/agent-console` returned HTTP 200, and `DSA_SIM` remained connected with its event bridge registered under listener PID `26608` (launcher PID `9084`). This read-only smoke submitted no order.
- Return/risk objective validation on 2026-07-15
  - `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`: 168 passed and 1 optional-runtime test skipped.
  - Focused Web API, Agent-console, and paper-trading suites: 73 passed. Lint reported no errors and retained the existing `SettingsPage.tsx:553` hook warning; the production build passed.
  - The final restarted Python 3.13.14 service reports build `local-return-risk-objective-final` under listener PID `22152` (launcher PID `13768`), connected `DSA_SIM`, an available MainEngine bridge, and four registered event callbacks. The read-only cross-run endpoint returned schema 3 plus `candidate-return-risk-v1`; `/agent-console` returned HTTP 200. Agent run count remained 2, the latest run UID and last-auto-run timestamp were unchanged, and no order was submitted.
- Return/risk calibration-trends validation on 2026-07-15
  - `python -m pytest tests/test_stock_selection_agent_backtest_service.py tests/test_stock_selection_agent_calibration_trends.py tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`: 169 passed and 1 optional-runtime test skipped.
  - Focused Web API and paper-trading suites: 59 passed. Lint reported no errors and retained the existing `SettingsPage.tsx:553` hook warning; the production build passed.
  - The restarted Python 3.13.14 service reports build `local-return-risk-calibration-final` under listener PID `23028`, connected `DSA_SIM`, and four registered event callbacks. The 30-day read-only calibration endpoint returned `collecting`, with 2 scanned legacy runs, 0 observed objective snapshots, and 2 `unknown`; it also reported overlapping rolling samples and made no independent-sample claim. `/paper-trading` returned HTTP 200. Agent run count remained 2, the latest run UID remained `ss-agent-20260714013029-481a1aeb`, and no order was submitted.
- Candidate-context provider routing validation on 2026-07-16
  - `python -m pytest tests/test_fundamental_context.py tests/test_alphasift_api.py tests/test_vnpy_paper_trading_api.py tests/test_vnpy_paper_trading_service.py -q -p no:cacheprovider`: 324 passed and 4 optional-runtime tests skipped.
  - Request-scoped fund-flow ordering, route-specific caching, provider-level trend persistence, quality-score non-duplication, API trend output, and pre-run versus post-run route audit semantics are covered. Python compilation, high-risk Flake8 rules, and `git diff --check` passed.
- News-provider routing validation on 2026-07-16
  - Search-provider, concurrency-cache, news-freshness, persisted source-health, AlphaSift, fundamental-context, and paper-trading API/service suites: 477 passed and 6 optional/network tests skipped.
  - Request-scoped provider ordering, explicit-route cache isolation, exception fallback, provider attempt-chain persistence, cached-attempt non-replay, 30-day health weighting, current quality-score non-duplication, three-failure circuit opening, circuit-open skipping, one-probe half-open recovery, and cross-restart state restoration are covered. Python compilation, high-risk Flake8 rules, and `git diff --check` passed.
  - The restarted Python 3.13.14 service reports build `local-news-provider-recovery-status-final` under listener PID `23180` (parent PID `11268`), connected `DSA_SIM`, four registered event callbacks, and a running reconnect monitor. `/api/v1/alphasift/status` reports news providers `tavily,searxng`, failure threshold 3, 300-second cooldown, one half-open call, and `cross_process_persistence=true`. Agent run count/latest UID remained 8 / `ss-agent-20260716081103-27d104bb`, submitted count remained zero, and the next automatic buy remains aligned to `2026-07-17T09:30:00` Asia/Shanghai.
- Agent-console news-provider observability validation on 2026-07-16
  - `cd apps/dsa-web && npm.cmd test -- --run src/pages/__tests__/AgentConsolePage.test.tsx`: 15 passed, covering effective provider order, open/closed state, failure count, cooldown, threshold, half-open allowance, cross-restart persistence, and the next recommended route.
  - `cd apps/dsa-web && npm.cmd run lint && npm.cmd run build`: lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the production TypeScript/Vite build passed.
  - No new browser screenshot was captured because the installed in-app Browser plugin is missing its required `scripts/browser-client.mjs` runtime file. The deterministic component interaction test, production build, and final live HTTP smoke are used as replacement evidence. The latest persisted Agent run predates news-route audit fields, so a real run carrying the new block remains pending rather than being synthesized for visual evidence.
  - The final restarted Python 3.13.14 service reports build `local-news-provider-web-final` under listener PID `18860` (launcher PID `27136`), connected `DSA_SIM`, four registered event callbacks, and a running reconnect monitor. `/agent-console` returned HTTP 200; AlphaSift reports news priority `tavily,searxng`, failure threshold 3, 300-second cooldown, one half-open call, and cross-process persistence enabled. Agent run count/latest UID remained 8 / `ss-agent-20260716081103-27d104bb`, the latest submitted count remained zero, and no order was triggered by this read-only smoke.
- Side-aware A-share historical stamp-duty validation on 2026-07-17
  - `python -m pytest tests/test_stock_selection_portfolio_backtest_service.py tests/test_alphasift_replay_api.py -q -p no:cacheprovider`: 28 passed. The cash ledger covers the official 2005-01-24, 2007-05-30, 2008-04-24, 2008-09-19, and 2023-08-28 boundaries, includes buy-side tax in whole-lot target sizing and cash affordability, audits the policy source per trade, and fails closed before the covered range.
  - `cd apps/dsa-web && npm.cmd test -- --run src/api/__tests__/alphasift.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`: 32 passed. The Agent console renders the historical coverage start and effective buy/sell bps schedule returned by the backtest methodology.
  - Web lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the production TypeScript/Vite build passed. Python compilation, high-risk Flake8 rules, and `git diff --check` passed. No screenshot was captured because the installed Browser plugin still lacks its required runtime file; deterministic component interaction tests and the production build are the replacement evidence.
  - The final restarted Python 3.13.14 service reports build `local-historical-stamp-duty-final` under listener PID `3052` (launcher PID `6792`), connected `DSA_SIM`, four registered event callbacks, and a running reconnect monitor. `/agent-console` returned HTTP 200; a read-only runtime resolver smoke returned buy-side 30 bps on 2007-05-30, buy-side 0 bps on 2008-09-19, and sell-side 5 bps on 2023-08-28 with their expected policy sources. The latest Agent run remained `ss-agent-20260716081103-27d104bb` with submitted count zero, and no order was triggered.
- Versioned portfolio cost-profile validation on 2026-07-17
  - `python -m pytest tests/test_stock_selection_portfolio_backtest_service.py tests/test_alphasift_replay_api.py -q -p no:cacheprovider`: 29 passed. A non-custom profile atomically overrides all requested cost fields, rejects a CN-only profile on other markets, and audits requested/effective assumptions plus `dsa-cost-profile-v1`.
  - `cd apps/dsa-web && npm.cmd test -- --run src/api/__tests__/alphasift.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`: 32 passed. The API maps profile and custom commission/slippage fields, while the Agent console selects a profile, locks overridden controls, and renders the backend-effective assumptions.
  - Web lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the production TypeScript/Vite build passed. Python compilation, full Flake8 on changed Python files, and `git diff --check` passed. Browser screenshot capture remains unavailable because the installed plugin lacks its required runtime file; component interaction tests and the production build are the replacement evidence.
  - The final restarted Python 3.13.14 service reports build `local-cost-profiles-final` under listener PID `9080` (parent PID `5980`), connected `DSA_SIM`, four registered event callbacks, and a running reconnect monitor. `/agent-console` returned HTTP 200; OpenAPI defaults to `custom` and exposes all four profile values. A read-only runtime smoke expanded retail reference to 3 bps/CNY 5/historical tax/5 bps slippage, low-commission reference to 1 bps with the same remaining assumptions, and zero-cost baseline to all-zero explicit costs under `dsa-cost-profile-v1`. The latest Agent run remained `ss-agent-20260716081103-27d104bb` with submitted count zero, and no order was triggered.
- Valuation-source health trend validation on 2026-07-17
  - `python -m pytest tests/test_vnpy_paper_trading_api.py -q`: 45 passed, including 15-minute bucket de-duplication, 7/30/90-day aggregation, provider ranking, empty-history behavior, full-status integration, sanitized persistence failure, and lightweight read-only behavior. The paper-trading page component suite passed 27 tests; lint had no errors and only the pre-existing `SettingsPage.tsx:553` warning; the production TypeScript/Vite build passed.
  - No browser screenshot was captured because the installed Browser plugin still lacks its required `scripts/browser-client.mjs` runtime file. Component interaction tests, the production build, and live HTTP checks provide replacement evidence.
  - The final restarted Python 3.13.14 service reports build `valuation-health-weighted-20260717` under listener PID `1852` (launcher PID `10704`), connected `DSA_SIM`, an available MainEngine bridge, all four order/trade/account/position event mappings, and a running reconnect monitor. The 7-day trend contains two effective snapshots, zero empty snapshots, and 16 position observations; weighted/time-average coverage and freshness are all 100%, Tencent share is 100%, and provider-attribution coverage is 100%. A lightweight status read returned in 84 ms and kept the observation count at two. `/paper-trading` and `/agent-console` returned HTTP 200. The latest Agent run remained `ss-agent-20260716081103-27d104bb` with submitted count zero, and no order was triggered.
- Sell-first gate and split-aware holding-age validation on 2026-07-17
  - `python -m pytest tests/test_vnpy_paper_trading_service.py -q`: 153 passed and 4 optional tests skipped. Coverage includes complete 100-row trade/split-history pagination, FIFO holding-age reset after full exit/re-entry, persisted same-day split/partial-exit replay, unavailable split-history fail-closed behavior, stop-loss preservation across data-quality and account/market buy gates, and persisted run-count consistency.
  - Python compilation, full Flake8 on the changed service/test files, and `git diff --check` passed; no Web code changed in this increment.
  - The restarted Python 3.13.14 service reports build `split-aware-holding-age-20260717` under listener PID `24492` (launcher PID `25900`), connected `DSA_SIM`, an available MainEngine bridge, four event callbacks, and a running reconnect monitor. Full paper status is available with eight current positions; `/paper-trading` and `/agent-console` returned HTTP 200. System health is warning outside the current trading window. The latest Agent run remained `ss-agent-20260716081103-27d104bb` with submitted count zero, so the read-only validation created no run or order.
- Agent portfolio-change audit validation on 2026-07-17
  - `python -m pytest tests/test_vnpy_paper_trading_service.py tests/test_vnpy_paper_trading_api.py -q`: 198 passed and 4 optional tests skipped. Coverage distinguishes Portfolio-ledger-backed fills from submitted or order-level partial reports, and verifies late fill refresh, dry-run plans, API serialization, and timeline output.
  - `cd apps/dsa-web && npm.cmd test -- src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/AgentConsolePage.test.tsx src/pages/__tests__/VnpyPaperTradingPage.test.tsx`: 76 passed. Web lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript/Vite production build passed.
  - Python compilation, full Flake8 on changed Python files, and `git diff --check` passed. No browser screenshot was captured because the installed in-app Browser plugin still lacks its required `scripts/browser-client.mjs` runtime file; component interaction tests, the production build, and final live HTTP checks are used as replacement evidence.
  - The restarted Python 3.13.14 service reports build `agent-portfolio-change-20260717` under listener PID `11052` (launcher PID `6032`), connected `DSA_SIM`, an available MainEngine bridge, four event callbacks, a running reconnect monitor, and eight current positions. `/paper-trading` and `/agent-console` returned HTTP 200. The existing latest run `ss-agent-20260716081103-27d104bb` dynamically returned `portfolio_change.status=unchanged`, zero changed symbols, and a portfolio-change timeline event; its submitted count and the runtime's latest-run UID/count remained zero and unchanged, so the smoke created no run or order. System health remains warning before the trading window opens.
- Candidate decision audit-contract validation on 2026-07-17
  - Five focused migration/service/API tests passed, covering legacy database column creation, old-row `order_result` fallback, dry-run top-level fields, EventEngine fill preservation, dedicated-column persistence, and API serialization. The broader storage + vn.py service/API run completed 218 passes and 4 optional skips; two pre-existing `test_storage.py` cases failed only while deleting already-validated temporary SQLite files on Windows with `WinError 32`. All 198 vn.py service/API outcomes passed.
  - `cd apps/dsa-web && npm.cmd test -- src/api/__tests__/vnpyPaperTrading.test.ts src/pages/__tests__/AgentConsolePage.test.tsx`: 49 passed, including snake-case mapping, top-level audit-field preference, and embedded legacy fallback. Web lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript/Vite production build passed.
  - Python compilation, high-risk Flake8 checks (`E9,F63,F7,F82`), and `git diff --check` passed. Full Flake8 still reports unrelated pre-existing whitespace and `F541` findings in `src/storage.py` / `tests/test_storage.py`. No browser screenshot was captured because the installed in-app Browser plugin still lacks its required `scripts/browser-client.mjs`; component tests, the production build, and final live HTTP/schema checks are used instead.
  - The restarted Python 3.13.14 service reports build `candidate-audit-contracts-20260717` under listener PID `16640` (launcher PID `20152`), connected `DSA_SIM`, an available MainEngine bridge, four event callbacks, and a running reconnect monitor. Startup and historical detail reads succeeded against the existing database, proving the new columns are available; OpenAPI exposes all five top-level decision fields. The latest legacy run restored non-empty `position_plan`, `risk_review`, and `agent_review` from its embedded payload, while absent historical strategy/LLM details correctly remained empty. Five lightweight status requests took 10~79 ms, and `/paper-trading` plus `/agent-console` returned HTTP 200. Latest run UID `ss-agent-20260716081103-27d104bb` and submitted count zero remained unchanged, so the smoke created no run or order; system health remains warning before the trading window opens.

- Risk-rejection and Web full-gate validation on 2026-07-17
  - `python -m pytest tests/test_vnpy_paper_trading_api.py -q -p no:cacheprovider`: 46 passed. The new HTTP-level matrix saves settings, runs five fixed candidates, rejects blacklist, ST/delisting, suspension, price-limit, and insufficient-liquidity cases in one Agent run, persists the same reasons in decisions and trade plans, and verifies that the Portfolio ledger remains empty.
  - `cd apps/dsa-web && npm.cmd test -- --run`: 91 files passed, 980 tests passed, and 2 tests were skipped. The stale JP/KR alert-option assertion now follows the backend-supported CN/HK/US boundary, the first-run setup smoke enters the required base category, and the comprehensive paper-trading render test has an explicit integration-test timeout.
  - Web lint completed with no errors and the pre-existing `SettingsPage.tsx:553` hook warning; the TypeScript/Vite production build passed. Browser screenshot capture remains unavailable because the installed Browser plugin lacks its required runtime file; deterministic component tests and the production build are the replacement evidence.
- Gateway reconnect-soak acceptance validation on 2026-07-17
  - `python -m pytest tests/test_vnpy_gateway_soak.py -q -p no:cacheprovider`: 6 passed. Reconnect acceptance independently checks disconnect injection, reconnect attempt, reconnect success, and final connected state; compilation and full Flake8 passed.
  - A Python 3.13.14 DSA_SIM smoke injected disconnect at 1 second, observed one reconnect attempt and one success, transitioned back to connected at 5.25 seconds, ended connected, and returned success without placing an order. This validates the schema-v2 harness, not a real broker gateway.
- Deployed vn.py runtime soak validation on 2026-07-20
  - `python -m pytest tests/test_vnpy_deployed_runtime_soak.py -q -p no:cacheprovider`: 4 passed across healthy acceptance, the complete failure matrix, intermittent callback registration, and a live HTTP-contract fixture; compilation and full Flake8 passed.
  - A 75-second read-only run against the Python 3.13.14 API recorded 75/75 successful samples and 100% API/runtime/connected/event-bridge/four-callback ratios, with contract v3, one stable process identity, no response errors, and no new reconnect failures. It did not create an Agent run, plan, or order.

## Unfinished Goals

- Real vn.py gateway validation is still not complete: the isolated runtime, event lifecycle, and built-in `DSA_SIM` reconnect/cache/exactly-once fill behavior are verified, but no concrete gateway plugin/account has been configured, so broker/paper acknowledgements, real-gateway reconnect behavior, and long-running subscriptions remain unverified.
- Basic cross-currency valuation, first-trade on-demand FX refresh, target replenishment, and covariance target-tracking allocation are complete for supported paper-trading markets. CN/US native AlphaSift paths and the DSA-owned HK compatibility path are operational; live-gateway validation remains incomplete.
- The Agent workflow is usable but not a complete autonomous research loop: run-level human acceptance, context reuse, execution-effective cross-market dynamic objectives, versioned daily return/risk utility, persisted 7/30/90-day calibration trends, long-horizon rule/LLM review-quality evaluation, and mature-quality calibration/gating are complete. Long-running production-sample and cross-market calibration evidence still needs more work.
- Background-task long-window metrics and cross-run Agent data-quality trends are available for 7/30/90-day windows. Fine-grained snapshot and candidate-context provider degradation rates are visible; automated snapshot, fund-flow, and news routing consume the 30-day same-strategy/market trend. Long-running production evidence across configured provider combinations remains pending.
- Persisted Agent candidates have strict forward multi-horizon evaluation; Tushare-first lifecycle metadata with a complete BaoStock+BSE official fallback reconstructs each dated A-share universe and feeds lease-protected resumable factor ingestion. Implemented dividend/share-adjustment events use Tushare first and a verified EastMoney/CNInfo free fallback for SSE/SZSE, are stored alongside ingestion, and are consumed by replay by default. Explicit request events can override stored events and provide an actual fractional-share cash-in-lieu settlement price. A-share stamp duty resolves by trade side and date from 2005-01-24 through the 2007, 2008, and 2023 official changes; dates before that coverage fail closed. Versioned retail-reference, low-commission-reference, and zero-cost profiles are available, while actual broker terms remain explicit `custom` input and are not inferred. Full-market factor completion and implemented SSE/SZSE corporate actions are independent of Tushare permission; BSE corporate-action coverage remains explicit and unresolved.
- Recovery now reconciles active MainEngine snapshots before timeout, survives service reconstruction in repository-backed tests, accepts late fills, and fails closed on query errors. The built-in simulator now has deterministic reconnect/cache/late-fill evidence; equivalent real-gateway reconnect and long-running lifecycle validation remain incomplete.

See [online-stock-selection-agent-goals.md](online-stock-selection-agent-goals.md) for the fuller goal breakdown.
